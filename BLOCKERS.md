# Open Studio Platform External Blockers

Date: 2026-05-19
Owner: Agent 4 release/signing/distribution scope

These items cannot be completed fully from the local repository. The
implementation artifacts and Cloudflare launch infrastructure are present, but
Windows OS signing certificates, hosted OS runner evidence, marketplace
publication, release checksum signing custody, and full cross-OS GA release
binaries must still be provisioned before public GA. A local Apple Developer ID
Application certificate now exists for the individual Apple Developer team and
has been used for macOS Apple Silicon v0.1.0 signing/notarization while the
account is later migrated to AuraOne.

Code-side status as of 2026-05-13: `pnpm --dir opensource/open-studio-platform run verify:code-gates`
passes. Cloudflare launch status as of 2026-05-19: update, intake, install,
schema, and telemetry Workers/Pages/routes are deployed; R2-backed stable
updater manifests for Rubric, Robotics, and Agent, the release key, and live
installer `.asc` signatures are published. The remaining entries below are hosted-runner, hardware,
credential, marketplace, artifact, or launch-evidence blockers rather than
missing local implementation harnesses. A 2026-05-19 local security refresh is
recorded in `docs/evidence/product/open-studio-local-security-release-preflight-2026-05-19.md`:
`security:all`, release-flow contract tests, Semgrep, current-tree gitleaks,
cargo audit, Linux package matrix, winget live preflight, and release-blocker
preflight ran locally; only the full git-history gitleaks gate remains open
with 33 redacted historical findings.

Cloudflare closure evidence as of 2026-05-14:

- `updates.auraone.ai/*` and `updates2.auraone.ai/*` route to Worker `auraone-open-update-server` version `3da276c6-9290-4a58-9526-1b3d68d99496`.
- `intake.auraone.ai/*` routes to Worker `auraone-open-intake-receiver` version `c3f17adf-2f0c-4a96-8716-c6896c3ffcb4`, with producer binding to Cloudflare Queue `auraone-open-cloud-import`.
- `install.auraone.ai/*` routes to Worker `auraone-open-install-server` version `5cbbd2a1-7496-4f7e-bf9d-ab8f25876e7c`.
- `schemas.auraone.ai` is active on Cloudflare Pages with SSL enabled and serves the intake packet schema.
- R2 buckets `auraone-open-updates` and `auraone-open-intake-packets` exist, and KV namespace `auraone-open-install-signatures` is bound in the install Worker config.
- `o.auraone.ai/*` routes to Worker `auraone-open-telemetry-receiver` version `79252c59-6624-4d1d-a213-dbc00532a851`; public DNS and Cloudflare-IP-pinned health/upload checks pass. This machine's local macOS resolver still had a transient negative cache for normal `getaddrinfo` immediately after the DNS record was created.
- AuraOne Open release GPG fingerprint is `F909 806D 13D9 CD4C F403 FA3C 8C61 E177 EB63 29E7`; the public key is served at `https://updates.auraone.ai/keys/auraone-open.gpg`.
- Tauri updater public key is `DAKD/Nqj4KoXZpXv9li/zVQv+2LhThXE5J9tx0Wl1B8=`.
- Rubric Studio Open, Robotics Studio Open, and Agent Studio Open `0.1.0` macOS aarch64 stable signed update manifests are published through R2/update Worker and attached to GitHub Releases as `latest.json` and `darwin-aarch64.json`.
- Installer script `.asc` signatures are live for Rubric, Robotics, and Agent. The signatures verify against the currently rendered installer scripts with release GPG fingerprint `F909 806D 13D9 CD4C F403 FA3C 8C61 E177 EB63 29E7`.

Apple Developer and GitHub release closure evidence as of 2026-05-19:

- Apple Developer Team ID is `6T44QQW78K` under the individual membership `Gurbaksh Chahal`.
- App identifiers are registered for `ai.auraone.rubricstudio.open`, `ai.auraone.roboticsstudio`, and `ai.auraone.agentstudio`.
- Developer ID Application certificate `Developer ID Application: Gurbaksh Chahal (6T44QQW78K)` is installed in the local login keychain, issued by Apple's Developer ID G2 CA, and expires on 2031-05-16.
- Local codesign smoke passed with hardened runtime and timestamp using Team ID `6T44QQW78K`.
- Local Apple Silicon `.app` and `.dmg` artifacts for Rubric Studio Open, Robotics Studio Open, and Agent Studio Open were built and signed with Team ID `6T44QQW78K`; `codesign --verify --deep --strict` passes for each `.app`, and `codesign --verify` passes for each `.dmg`.
- App Store Connect API access was approved, and the local notary key `AuraOne Notary 2026` was created for Key ID `RBBDYVWC26` and Issuer ID `dbf64d78-fa48-46b6-9536-5bdd89901176`; the private key is stored outside the repo under the local signing directory.
- Apple notarization submissions are accepted for the final Rubric, Robotics, and Agent macOS Apple Silicon DMGs. Each DMG was stapled, `xcrun stapler validate` passed, and `spctl -a -vvv -t install` accepted each artifact with `source=Notarized Developer ID`.
- GitHub Releases `v0.1.0` now carry the final notarized macOS DMGs, signed `SHA256SUMS`/`SHA256SUMS.asc` assets, signed updater manifests, product-scoped CycloneDX release SBOM assets, and the generated Open Studio Platform npm CycloneDX SBOM asset `Open.Studio.Platform_0.3.0_npm.cdx.json` for `auraoneai/rubric-studio-open`, `auraoneai/robotics-studio-open`, and `auraoneai/agent-studio-open`. Available registry-style artifacts are also attached: Rubric VSIX plus `rubric-spec` wheel/sdist, Robotics `robostudio-engine` wheel/sdist, and Agent VSIX plus `agentstudio` wheel/sdist.
- Published macOS DMG SHA-256 values are:
  - Rubric Studio Open: `e9851568e64a8315e6b72411fb1fa8d6d4d0131fa15882478d29f83cc548c3c3`
  - Robotics Studio Open: `acdc4c53457ea85a8c306728f82cc07a0eb5b5b0423bf719f42f012e19a113d3`
  - Agent Studio Open: `1dce9e2abab65a5ef08114f0eeb64c76c4ac1784d5ddc8cfb67f53f8302bb8b7`
- Release-blocker preflight is now machine-checkable with
  `pnpm --dir opensource/open-studio-platform run verify:release-blockers -- --probe-uptime`.
  The current 2026-05-20 state confirms: Rubric, Robotics, and Agent have
  notarized macOS DMGs, signed Linux AppImage/deb/rpm assets for the current
  matrix, detached `.asc` signatures, per-artifact `.sha256` files,
  package-repository metadata, clean-install evidence, shared Platform SBOMs,
  and product-scoped release SBOM assets. Agent also has one public MSI, and
  Microsoft Store package identity plus publisher-verification evidence is
  accepted for all three flagships, but no flagship has EV-signed Windows
  package evidence. Hosted release custody has Apple notary, GPG, updater, release-bot,
  Cloudflare API token/account ID, R2 bucket, and Sentry runtime DSN secrets
  configured; Windows signing custody is the remaining hosted secret gap.
  Rubric uptime target endpoints are reachable and provider monitors are now
  provisioned in Sentry Uptime.

| Area | Blocker | Required next action | Local artifact ready |
|---|---|---|---|
| macOS CI signing custody | Local Apple Developer Team ID, Developer ID Application certificate, accepted notarization, stapling, Gatekeeper validation, and GitHub Release upload are complete for the v0.1.0 Apple Silicon DMGs. App Store Connect API-key notarization secrets are now configured in the checked root/public repositories, but CI Developer ID certificate import/HSM signing and hosted release execution are not yet proven. | Move Developer ID signing custody into CI via approved P12 import or HSM-backed runner, then run the hosted release workflow for all three flagships. | `scripts/sign-macos.sh`, `scripts/notarize.sh`, `scripts/build-signed-macos-flagships.sh`, release workflow template, `scripts/verify-signing-custody-readiness.mjs`, `docs/evidence/product/open-studio-signing-custody-preflight-2026-05-19.md` |
| Windows signing | Windows EV code-signing certificate/HSM, PFX, or managed Azure Artifact Signing custody is not available locally. Rubric and Robotics have no public MSI release assets; Agent has `Agent.Studio.Open_0.1.0_x64_en-US.msi`, but no EV-signing evidence is present for winget acceptance. Microsoft Store onboarding is complete through the approved temporary individual publisher account, and Partner Center EXE/MSI identities are reserved for `AuraOne.RubricStudioOpen`, `AuraOne.RoboticsStudioOpen`, and `AuraOne.AgentStudioOpen`. Signing-custody and Windows identity preflights still confirm no local Windows signing env, no hosted Windows signing secrets, no signed-MSI evidence, and no clean Windows install evidence. | Provision EV cert or managed Azure Artifact Signing account/certificate profile, install the certificate on a controlled signing runner/HSM, set `AURAONE_WINDOWS_CERT_THUMBPRINT`, the PFX secret pair, or `AURAONE_WINDOWS_SIGNING_PROVIDER=azure-artifact-signing` with DLIB plus metadata or endpoint/account/profile secrets, then sign and verify canary MSI/MSIX artifacts for all three flagships and attach signed-MSI/install evidence under `AURAONE_WINDOWS_IDENTITY_EVIDENCE_DIR` or the committed fallback directory. | `scripts/sign-windows.ps1`, winget templates, `scripts/verify-release-blocker-preflight.mjs`, `scripts/verify-signing-custody-readiness.mjs`, `scripts/verify-windows-package-identity-readiness.mjs`, `distribution/windows/windows-package-identity-readiness.json`, `docs/evidence/product/windows-package-identity/`, `docs/evidence/product/open-studio-signing-custody-preflight-2026-05-19.md`, `docs/evidence/product/open-studio-windows-package-identity-readiness-2026-05-19.md` |
| Linux package and checksum signing | Complete for the current v0.1.0 Linux artifact matrix. The AuraOne Open public release GPG key is published, local release GPG custody is available outside the repo, `SHA256SUMS.asc` is published for the current release checksum sets, and Rubric, Robotics, and Agent all have signed AppImage/deb/rpm assets with detached `.asc` signatures, `.sha256` files, signed package-repository metadata, signature verification, and Ubuntu/Fedora clean-install evidence. `verify:linux-artifacts` reports `readyForLinuxArtifactClosure: true`. Hosted release-workflow reproduction remains tracked under the release-custody row. | Keep Linux artifact generation and package-repository publication in the protected hosted release workflow, then record the hosted reproduction evidence when the release workflow runs. | `scripts/sign-linux.sh`, `scripts/generate-checksums.sh`, install scripts, `scripts/verify-release-blocker-preflight.mjs`, `scripts/verify-signing-custody-readiness.mjs`, `scripts/verify-linux-artifact-readiness.mjs`, `distribution/linux/linux-artifact-readiness.json`, `docs/evidence/product/open-studio-signing-custody-preflight-2026-05-19.md`, `docs/evidence/product/open-studio-linux-artifact-readiness-2026-05-19.md` |
| Tauri updater signing custody | The updater keypair exists, public key is wired into configs, signed stable manifests are live for all three macOS releases, and `AURAONE_UPDATE_SIGNING_KEY_PEM` is now configured as a GitHub Actions secret in the checked root/public repositories. Hosted manifest signing custody remains open until a hosted signing canary runs and rotation evidence is recorded. | Run a hosted update-manifest signing canary from the protected release environment and record rotation/audit evidence. | `scripts/make-updater-manifest.mjs`, update Worker, `scripts/verify-signing-custody-readiness.mjs`, `docs/evidence/product/open-studio-signing-custody-preflight-2026-05-19.md` |
| GitHub Releases | The v0.1.0 macOS notarized DMGs, signed `SHA256SUMS` assets, shared Open Studio Platform npm SBOM, product-scoped release SBOMs, updater manifests, VSIX artifacts, Python wheel/sdist artifacts where applicable, and signed Linux AppImage/deb/rpm assets are published for Rubric, Robotics, and Agent. Full cross-OS signed GA assets are still pending. Public Agent PR #8 and Robotics PR #34 are merged and harden public release workflow custody; Agent PR #11 and Robotics PR #35 are also merged and harden release publish custody/update-manifest publication. The public publish paths require release-bot and Cloudflare account/token custody, and the local Robotics workflow copy is patched. Signing-custody preflight confirms release-bot, Cloudflare API token/account-ID, and R2 bucket-name secrets are configured across the checked root/public repositories; Windows signing custody remains the hosted secret gap and can close through thumbprint, PFX, or managed Azure Artifact Signing provider secrets. | Configure the remaining hosted Windows signing custody, then run the hosted release workflow and attach the final cross-OS signed GA asset set. | `.github-templates/workflows/release.yml`, `scripts/publish-release.sh`, `scripts/generate-product-release-sboms.mjs`, `scripts/verify-signing-custody-readiness.mjs`, `docs/evidence/product/open-studio-signing-custody-preflight-2026-05-19.md` |
| DCO and CODEOWNERS enforcement | Public flagship CODEOWNERS teams now exist and have repo access for Robotics, Rubric, and Agent. Agent, Rubric, and Robotics CODEOWNERS are merged, and branch protection on all three public repos requires two approving reviews plus CODEOWNERS review. Robotics PRs #33/#34/#35 and Agent PRs #8/#9/#10/#11 merged with passing `DCO / dco` checks after the open branches were patched with the same workflow/guidance. The repeatable `verify:dco-enforcement` gate now records branch protection, workflow-file, and tracked public PR state; the remaining enforcement gap is root branch protection, which is unverified from this account due to the private-repository plan API limitation. | Map platform-owner teams for the platform repo, and attach root branch-protection or DCO App evidence. | `CODEOWNERS`, `docs/evidence/product/open-studio-cross-repo-codeowners-routing-2026-05-19.md`, `docs/evidence/product/open-studio-dco-enforcement-2026-05-20.md`, `docs/evidence/product/robotics-codeowners-review-routing-2026-05-19.md`, `pnpm --dir opensource/open-studio-platform run verify:dco-enforcement` |
| Homebrew tap | Complete for macOS v0.1.0 casks. `auraoneai/homebrew-open` now publishes Rubric, Robotics, and Agent casks pointing at the notarized Apple Silicon DMGs and final SHA-256 values. | Keep the tap in the release workflow so future cask bumps are automated. | `distribution/homebrew/Casks/*.rb`, `auraoneai/homebrew-open` |
| winget | winget package submission cannot be completed without EV-signed public MSI artifacts, real product codes, winget validation/submission evidence, and clean Windows install evidence. Live preflight confirms Rubric and Robotics are missing x64/arm64 MSI release assets, while Agent has a public x64 MSI whose manifest SHA matches the GitHub release digest but still lacks EV-signing evidence for winget acceptance. The Windows package identity preflight confirms `readyForWindowsPackageIdentityClosure: true` and `readyForWingetSubmission: false`; Rubric/Robotics staged winget manifests still carry placeholder SHA/ProductCode values. | Produce signed Rubric/Robotics MSI artifacts, attach EV-signing evidence for all three public Windows artifacts, replace all placeholder SHA/ProductCode values, validate and submit manifests to winget-pkgs under publisher `AuraOne`, and capture clean Windows install QA. | `distribution/winget/**`, `distribution/windows/windows-package-identity-readiness.json`, `scripts/verify-winget-matrix.mjs`, `scripts/verify-windows-package-identity-readiness.mjs`, `docs/evidence/product/windows-package-identity/`, `docs/evidence/product/open-studio-winget-live-preflight-2026-05-19.md`, `docs/evidence/product/open-studio-windows-package-identity-readiness-2026-05-19.md` |
| Sentry telemetry/crash provisioning | Complete for the current launch evidence. The Sentry org `auraone-open` contains `rubric-studio-open`, `robotics-studio-open`, and `agent-studio-open`; DSNs were validated from an out-of-repo runtime env file, synthetic message/crash events were uploaded, and dashboard/issue-feed evidence is recorded. `sentry-cli` and `SENTRY_AUTH_TOKEN` remain optional for the API probe; browser evidence and the credential-safe verifier cover the launch gate. | Keep DSNs in release/runtime secret custody and run the optional `--probe-sentry` API proof when a Sentry token is available. | `configs/sentry/projects.json`, `configs/sentry/telemetry-dashboard-provisioning.md`, `scripts/verify-observability-readiness.mjs`, `scripts/verify-release-blocker-preflight.mjs`, `docs/evidence/product/open-studio-sentry-2026-05-20/`, `docs/evidence/product/open-studio-observability-readiness-2026-05-20.md` |
| Rubric uptime monitoring | Complete for the current launch evidence. `distribution/status/rubric-studio-open.json` is `provisioned` and records Sentry Uptime monitor URLs for the public status page, stable update manifest, docs custom domain, and intake health endpoint. The product-domain monitor is also live. Provider-side `Test Monitor` returned `Uptime check passed successfully` for each endpoint. | Watch the scheduled check-in history after launch and add screenshots/export if stricter post-launch SLA evidence is required. | `distribution/status/rubric-studio-open.json`, `scripts/verify-observability-readiness.mjs`, `scripts/verify-release-blocker-preflight.mjs`, `docs/evidence/product/open-studio-sentry-2026-05-20/rubric-studio-open-uptime-monitor.md`, `docs/evidence/product/open-studio-sentry-2026-05-20/rubric-studio-open-endpoint-uptime-monitors.md`, `docs/evidence/product/open-studio-observability-readiness-2026-05-20.md` |
| Intake Cloud import | Complete for the current launch path. The intake Worker stores packets, sends queue messages to `auraone-open-cloud-import`, and returns `cloud_url`; `auraone-open-cloud-import-consumer` is deployed in production with one active queue consumer, reads the queued R2 packet, posts the packet/manifest contract to `https://auraone.ai/api/open-studio/imports`, and writes audit evidence to `auraone-open-cloud-import-audit`. Live smoke packet `2cdef54f-932a-4c64-91de-a8dea53db0c9` reached terminal state `imported`, and `verify:cloud-import-consumer` reports `readyForCloudImportConsumerClosure: true`. | Keep the consumer deployed with the intake Worker and repeat the live smoke/audit capture whenever the packet schema or Cloud import API changes. | `services/intake-receiver`, `services/cloud-import-consumer`, `docs/evidence/product/open-studio-cloud-import/`, `pnpm run verify:cloud-import-consumer` |
| Install script signatures | Complete for the live Rubric, Robotics, and Agent shell installers, including the clean-keyring hardening. The install Worker version `759aca6c-f51a-48d3-8fe9-6c2583a260a9` now serves scripts that fetch/import the published AuraOne Open release public key after fingerprint verification when the key is absent, and fresh `.asc` signatures in `INSTALL_SIGNATURES` verify against the exact live script bytes for all three flagships. | Repeat render, detached-sign, KV upload, Worker deploy, and live-signature verification whenever installer script bytes change. | `services/install-server`, `installers/**`, `docs/evidence/product/open-studio-install-server-clean-keyring-2026-05-20.md` |
| Hosted Tauri template matrix | The macOS/Windows/Linux template workflow exists, repository Actions are enabled/allowed, and the root workflow plus reusable template now accept JSON runner-label overrides through workflow-dispatch inputs and `AURAONE_PLATFORM_*_RUNS_ON` repository variables. Sampled root run `https://github.com/gchahal1982/AuraFoundry/actions/runs/26167421304` still has all Tauri template jobs blocked before runner steps by the Actions-budget annotation because no external budget/runner state changed yet. | Restore GitHub Actions budget/credits or configure macOS, Windows, and Linux self-hosted runner label JSON through the workflow inputs/repository variables, then rerun and attach the macOS, Windows, and Ubuntu run URLs. | `templates/tauri-app`, `.github/workflows/open-studio-platform-hosted-smokes.yml`, `.github-templates/workflows/platform-code-verification.yml`, `docs/evidence/product/open-studio-hosted-matrix-2026-05-19.md`, `pnpm --dir opensource/open-studio-platform run verify:root-hosted-actions` |
| Native keychain smoke matrix | The ignored native smoke test exists and the hosted smoke workflow includes macOS, Windows, and Ubuntu keychain jobs with the same self-hosted runner override path. Run `26166265859` proved the macOS native keychain smoke once with 6 executed steps, but sampled root run `26167421304` could not start any native keychain job because of the Actions-budget blocker. | Restore runner availability or configure OS-specific self-hosted runner label JSON, rerun the native keychain matrix with `AURAONE_RUN_NATIVE_KEYCHAIN_SMOKE=1`, and attach macOS, Windows, and Ubuntu run URLs. | `crates/auraone-platform-keychain`, `.github/workflows/open-studio-platform-hosted-smokes.yml`, `.github-templates/workflows/platform-code-verification.yml`, `docs/evidence/product/open-studio-hosted-matrix-2026-05-19.md`, `pnpm --dir opensource/open-studio-platform run verify:root-hosted-actions` |
| GPU decode fixtures | The hardware fixture contract exists and the hosted smoke workflow includes VideoToolbox, VAAPI, and Direct3D11 jobs with GPU-specific runner override variables. Sampled root run `26167421304` still has all GPU fixture jobs blocked before runner steps by the Actions-budget blocker. No `AURAONE_GPU_FIXTURE` variable evidence is present. | Restore runner availability or configure GPU-capable self-hosted runner label JSON, mount the fixture path, set `AURAONE_RUN_GPU_DECODE_SMOKE=1` and `AURAONE_GPU_FIXTURE`, and run GPU-capable CI jobs. | `crates/auraone-platform-video`, `.github/workflows/open-studio-platform-hosted-smokes.yml`, `.github-templates/workflows/platform-code-verification.yml`, `docs/evidence/product/open-studio-hosted-matrix-2026-05-19.md`, `pnpm --dir opensource/open-studio-platform run verify:root-hosted-actions` |
| Robotics hosted/hardware readiness | The Robotics public CI, accessibility/performance, and security/SBOM/license runs are readable and mostly green, but the stricter Robotics hardware preflight reports `readyForRoboticsHostedHardwareClosure: false`: `CI / gpu-video-embedding` is skipped, `ROBOTICS_GPU_CI_ENABLED` is not configured, no online self-hosted GPU runner is registered, and target-hardware/14-day evidence is not attached. PR #33 merged, repairing the `macos-x64` lane on `macos-15-intel`; fresh `main` CI run `26158186545` passed the full required non-GPU OS matrix, including `CI / macos-x64`. Branch protection was restored on 2026-05-20 and now requires two reviews, CODEOWNERS, DCO, security/SBOM/license, accessibility/performance, and all declared PRD OS contexts including `CI / gpu-video-embedding`. | Enable the GPU runner and repo variable, attach M2 Pro/Linux x86/Windows 11 performance evidence plus 14-day green CI evidence, then rerun `verify:robotics-hosted-hardware`. | `distribution/robotics/robotics-hosted-hardware-readiness.json`, `scripts/verify-robotics-hosted-hardware-readiness.mjs`, `docs/evidence/product/robotics-hosted-hardware-readiness-2026-05-19.md`, `docs/evidence/product/robotics-codeowners-review-routing-2026-05-19.md` |
| GA checklist evidence | The checklist artifacts and local release-flow tests exist, but final GA artifacts do not. | Run per-flagship checklists against signed GA builds and attach reviewer sign-off. | `security/checklists/*-ga.md`, `tests/platform-release-flow.test.mjs` |
| Repository history secret scan | Current-tree gitleaks scans now pass for Open Studio Platform, Rubric, Robotics, and Agent. Full git-history gitleaks still scans 3180 commits and reports 33 redacted historical findings. | Remediate, rotate, purge, or approve a reviewed baseline for historical findings before making full-history secret scanning a required branch/release gate. | `configs/gitleaks/gitleaks.toml`, `docs/evidence/product/open-studio-local-security-release-preflight-2026-05-19.md` |

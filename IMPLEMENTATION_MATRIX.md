# Open Studio Platform Implementation Matrix

Last audited: 2026-05-20

Scope: `docs/prds/open-studio-platform-prd.md` plus cross-PRD alignment with Rubric Studio Open, Robotics Studio Open, and Agent Studio Open. Status is evidence-based. Items without a concrete artifact or verified live service remain unchecked in the PRD.

Concurrent work note: implementation artifacts for crates, templates, workflows, services, schemas, installers, and security/compliance scaffolds appeared during this audit. This matrix treats those as present but not complete unless the verification command listed here passed. The current blocker for most implementation items is verification, not absence.

Status legend:

- Complete: artifact exists in this workspace and local verification is listed.
- Pending: owned by another PRD agent or not yet implemented in this workspace.
- External blocker: depends on certificates, hosted OS runners, SaaS account, marketplace publication, GitHub App installation, third-party audit, or other external state.

## Current Verification Findings

| Command | Result |
|---|---|
| `test -f ...` governance file checks | Passed. |
| `find opensource/open-studio-platform -maxdepth 4 -type f` | Shows governance files plus concurrent implementation artifacts for templates, crates, schemas, services, workflows, installers, security/compliance, and distribution scaffolds. |
| JSON parse loop for schemas/configs/compliance JSON | Passed with `json-ok`. |
| `node opensource/open-studio-platform/tests/intake_roles_test.mjs` | Passed. |
| `bash -n` for shell signing/release helper scripts and `node --check` for updater helper scripts | Passed. |
| `cargo test --workspace` | Passed on 2026-05-13 after workspace dependency verification. |
| `pnpm --dir opensource/open-studio-platform test` | Passed on 2026-05-13: Rust workspace tests, platform contract tests, intake role tests, and Aura IDE kit Vitest suite. |
| `pnpm --dir opensource/open-studio-platform typecheck` | Passed on 2026-05-13 after adding platform-local pnpm workspace metadata and template/docs typecheck coverage. |
| `node opensource/open-studio-platform/scripts/verify-platform-contracts.mjs` | Passed on 2026-05-13. |
| `pnpm --dir opensource/open-studio-platform build` | Passed on 2026-05-13: platform contracts build, Aura IDE kit build, Docusaurus docs-template build. |
| `pnpm --dir opensource/open-studio-platform/templates/tauri-app run verify` | Passed on 2026-05-13: Vite production build and Tauri Rust `cargo check` for the local macOS target. |
| `pnpm --dir opensource/open-studio-platform/docs-template run build` | Passed on 2026-05-13. |
| `pnpm --dir opensource/open-studio-platform audit --prod --audit-level high` | Passed on 2026-05-13 after pinning `serialize-javascript >=7.0.3`; nine moderate advisories remain below the high gate. |
| `pnpm --dir opensource/open-studio-platform run security:licenses` | Passed on 2026-05-13 across 1104 pnpm-resolved packages after license policy review for MIT-0, BlueOak-1.0.0, and `require-like` MIT evidence. |
| `pnpm --dir opensource/open-studio-platform run verify:code-gates` | Passed on 2026-05-13: cargo-audit with expiring Tauri GTK/WebKit advisory acceptance, PowerShell syntax parse, platform code-verification workflow contract tests, flagship integration contract tests, no-network/crash/telemetry/intake release-flow contract tests, and Rust workspace tests. |
| `go run github.com/rhysd/actionlint/cmd/actionlint@latest ...` | Passed on 2026-05-13 for platform release, security, sync, and root open-studio release workflow templates. |
| `AURAONE_TELEMETRY_RESOLVE_IP=104.21.10.111 pnpm --dir opensource/open-studio-platform smoke:live` | Passed all live update/intake/install/schema/telemetry checks on 2026-05-14 while this machine's normal `getaddrinfo` still had a transient negative cache for newly-created `o.auraone.ai`. |
| `curl -fsSI https://updates.auraone.ai/healthz`, `https://updates2.auraone.ai/healthz`, `https://intake.auraone.ai/healthz`, `https://install.auraone.ai/rubric-studio-open.asc`, `https://schemas.auraone.ai/open-studio/intake-packet/v1.json` | Passed on 2026-05-14. |
| `curl -fsSL https://install.auraone.ai/<flagship>` | Passed on 2026-05-19 for Rubric, Robotics, and Agent after redeploying install Worker version `f08c3c8c-2387-4126-9e87-8724b02aacf6`; scripts resolve final notarized DMG filenames and enforce Gatekeeper verification. |
| PowerShell parse for `sign-windows.ps1` and Robotics installer | Passed on 2026-05-13 with local `pwsh` 7.6.1 via `node opensource/open-studio-platform/scripts/verify-powershell-syntax.mjs`. |
| `xcrun stapler validate` and `spctl -a -vvv -t install` for Rubric, Robotics, and Agent macOS DMGs | Passed on 2026-05-19. All three DMGs are stapled and accepted with `source=Notarized Developer ID`. |
| `gh release view v0.1.0 --repo auraoneai/<flagship> --json assets` | Passed on 2026-05-19 for Rubric, Robotics, and Agent. GitHub asset digests match the final notarized DMG SHA-256 values. |
| `pnpm --dir opensource/open-studio-platform run verify:observability -- --probe-uptime --probe-github-secrets` | Passed on 2026-05-20 with `readyForObservabilityClosure: true`; project, hosted DSN secret, synthetic upload, dashboard, and Rubric uptime evidence are present for all configured Open Studio products. |
| `pnpm --dir opensource/open-studio-platform run verify:website-screenshots` | Passed on 2026-05-20 with `readyForWebsiteScreenshotRouteClosure: true`; Agent/Rubric marketing walkthrough markers, local/live PNG screenshot dimensions, Robotics/Trust Toolkit routes, and the `/open/v2` redirect are verified against production. |
| `pnpm --dir opensource/open-studio-platform run verify:root-hosted-actions` | Passed on 2026-05-20 as a read-only preflight with `readyForRootHostedActionsClosure: false`; repository Actions are enabled and allowed. Historical run `26166265859` proved `Native keychain smoke (macos-14)` once, while sampled run `26167421304` failed all 10 hosted-smoke jobs before runner steps because the Actions-budget annotation prevented runner startup. The verifier also confirms `.github/workflows/open-studio-platform-hosted-smokes.yml` and `.github-templates/workflows/platform-code-verification.yml` now support dynamic `runs-on` JSON through workflow-dispatch inputs and `AURAONE_PLATFORM_*_RUNS_ON` repository variables for externally provided self-hosted runners. |

## Acceptance Criteria Coverage

| ID | Requirement | Owner | Status | Evidence | Verification |
|---|---|---|---|---|---|
| AC-21-01 | `opensource/open-studio-platform/` exists with `README.md`, `LICENSE` MIT, `CHANGELOG.md`, `CONTRIBUTING.md` with DCO, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `GOVERNANCE.md`. | Agent 1 | Complete | Root governance files in `opensource/open-studio-platform/`. | `test -f opensource/open-studio-platform/README.md && test -f opensource/open-studio-platform/LICENSE && test -f opensource/open-studio-platform/CHANGELOG.md && test -f opensource/open-studio-platform/CONTRIBUTING.md && test -f opensource/open-studio-platform/CODE_OF_CONDUCT.md && test -f opensource/open-studio-platform/SECURITY.md && test -f opensource/open-studio-platform/GOVERNANCE.md` |
| AC-21-02 | `templates/tauri-app/` produces a buildable Tauri 2.x app on macOS, Windows, Linux; loads AuraGlass; registers `auraone://`; ships canonical CSP. | Agent 2 | Code complete; hosted OS matrix execution pending | `templates/tauri-app/` has local pnpm workspace coverage, AuraGlass/Aura IDE kit dependency wiring, canonical CSP/deep-link/updater config, valid RGBA icon, no placeholder sidecar binary requirement, and `.github-templates/workflows/platform-code-verification.yml` defines macOS/Windows/Linux template verification with Linux WebKit dependencies. | `pnpm --dir opensource/open-studio-platform/templates/tauri-app run verify`; `node opensource/open-studio-platform/scripts/verify-platform-contracts.mjs`; `node --test opensource/open-studio-platform/scripts/test/platform-code-verification-workflow.test.mjs`; next action: run the hosted macOS/Windows/Linux matrix and attach run URLs. |
| AC-21-03 | `crates/auraone-platform-keychain` works on macOS Keychain, Windows Credential Manager, Linux Secret Service, Linux file fallback. | Agent 3 | Code complete; native OS smoke execution pending | Crate exposes all required backend kinds, an encrypted AES-256-GCM file fallback, target-specific `keyring` 3.6.3 native providers for macOS, Windows, and Linux Secret Service, approved secret scopes, current-OS backend selection coverage, and an ignored native round-trip smoke test wired into the OS matrix workflow. Live Keychain/Credential Manager/Secret Service smoke tests are still pending. | `cargo test --workspace`; `pnpm --dir opensource/open-studio-platform run verify:code-gates`; next action: run native backend smoke tests on macOS, Windows, and Linux Secret Service runners with `AURAONE_RUN_NATIVE_KEYCHAIN_SMOKE=1`. |
| AC-21-04 | `crates/auraone-platform-intake` writes a valid `.auraonepkg` zip with schema-valid `manifest.json`. | Agent 3 | Complete for local packet writer and transport contract | Intake crate writes signed ZIP packets, enforces privacy exclusions, includes manifest/payload/signature layout, supports HTTP/mTLS transport, and platform tests validate role/schema contracts. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test --workspace`; `pnpm --dir opensource/open-studio-platform test`; `pnpm --dir opensource/open-studio-platform verify`. |
| AC-21-05 | `crates/auraone-platform-updater` verifies a signed Tauri updater manifest against platform pubkey. | Agent 3 | Complete for signed manifest verifier and rollout logic | Updater crate verifies Ed25519 signed manifests, rejects tampering, validates AuraOne update URLs, supports rollout/kill-switch behavior, and release manifest generation now emits the same schema/signature fields consumed by Rust, TypeScript contracts, update Worker tests, and the Tauri template type. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test --workspace`; `pnpm --dir opensource/open-studio-platform verify:agent4-release-ops`; `node opensource/open-studio-platform/scripts/test/make-updater-manifest.test.mjs`. |
| AC-21-06 | `crates/auraone-platform-telemetry` enforces schema, scrubs forbidden fields, and surfaces an in-app event log component. | Agent 3 / Agent 2 | Code complete; external release evidence pending | Telemetry crate tests, TypeScript platform contracts, forbidden-field validation, shared event-log contract tests, and Rubric/Robotics/Agent settings UI wiring to `AuraTelemetryEventLog` pass. | `pnpm --dir opensource/open-studio-platform run verify:code-gates`; `node --test opensource/open-studio-platform/tests/flagship_integration_contracts_test.mjs`; next action: capture GA build screenshots/checklist evidence after release artifacts exist. |
| AC-21-07 | `crates/auraone-platform-crash` integrates with Sentry, scrubs, and respects opt-in default-off. | Agent 3 / Agent 5 | Complete for local crash contract; Sentry provisioned | Crash crate tests verify default-off behavior, Sentry posture encoding, path/API-key scrubbing, and sanitized local minidump storage when supported. The 2026-05-20 observability evidence proves Sentry projects, hosted DSN secrets, synthetic upload evidence, dashboard evidence, and Rubric uptime monitors are present; `verify:observability -- --probe-uptime --probe-github-secrets` returns `readyForObservabilityClosure: true`. Native OS crash fixture runs remain a separate hosted-runner follow-up. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test --workspace`; `pnpm --dir opensource/open-studio-platform test`; `pnpm --dir opensource/open-studio-platform security:all`; `pnpm --dir opensource/open-studio-platform run verify:observability -- --probe-uptime --probe-github-secrets`. |
| AC-21-08 | `scripts/sign-macos.sh`, `scripts/notarize.sh`, `scripts/sign-windows.ps1`, `scripts/sign-linux.sh` exist and are documented. | Agent 4 | macOS local signing/notarization complete; Linux GPG custody available; Windows custody pending | Signing scripts exist. Release GPG key and updater public key are generated/published/wired into configs. Bash/node syntax checks passed, and `sign-windows.ps1` plus the Robotics installer parse with local `pwsh` 7.6.1. Local Developer ID signing and notarization passed for all three macOS Apple Silicon DMGs using Team ID `6T44QQW78K`. Release `SHA256SUMS.asc` signatures are published for the current v0.1.0 checksum sets. The 2026-05-19/2026-05-20 signing-custody preflight detects local release GPG custody and confirms the checked GitHub repos have Apple notary API-key, release GPG private-key, updater-signing, release-bot, Cloudflare API token/account-ID, and R2 bucket secrets configured. Public Agent/Robotics release-custody PRs are merged. Microsoft Partner Center identity and publisher-verification evidence is accepted for all three flagships. Remaining custody gaps are Windows EV/PFX/managed-provider custody, hosted Developer ID import or HSM proof, signed-MSI evidence, clean Windows install evidence, and hosted signing-canary execution. | `node opensource/open-studio-platform/scripts/verify-powershell-syntax.mjs`; `pnpm --dir opensource/open-studio-platform run verify:signing-custody`; `pnpm --dir opensource/open-studio-platform run verify:windows-package-identity`; dry-run `sign-windows.ps1` and `sign-linux.sh`; next action: provision Windows EV/HSM/PFX or managed Azure Artifact Signing custody and run hosted signing canaries. |
| AC-21-09 | `.github-templates/workflows/release.yml` produces signed/notarized artifacts and pushes to GitHub Releases and Cloudflare R2. | Agent 4 | macOS/Linux GitHub/R2 assets published; hosted cross-OS GA pending | Workflow template exists and `actionlint` passes. Release ops verification covers signed manifest generation, installer script rendering, update-server/intake/install-server tests, helper syntax checks, and telemetry Worker tests. Rubric, Robotics, and Agent GitHub Releases `v0.1.0` now carry notarized macOS Apple Silicon DMGs, signed `SHA256SUMS`/`SHA256SUMS.asc` assets, shared Platform SBOM, product-scoped release SBOMs, signed `latest.json`/`darwin-aarch64.json` updater manifests, available VSIX/Python package artifacts where applicable, and signed Linux AppImage/deb/rpm assets with `.asc`/`.sha256` files plus package-repo metadata for the current supported matrix. R2 serves signed stable updater manifests for all three flagships. Public Agent PR #8 and Robotics PR #34 are merged and harden public release workflow custody with release-environment gates, AuraOne signing secret names, App Store Connect API-key notarization support, Linux GPG private-key import, detached package signatures, signed aggregate `SHA256SUMS.asc`, Cloudflare publish custody, release-bot custody, and artifact attestation where applicable. Follow-up Agent PR #11 and Robotics PR #35 are open to add Agent update-manifest publication, the missing Agent publish-job checkout, Robotics explicit publish-custody checks, R2 bucket env propagation, and Wrangler fallback; the local Robotics workflow copy is patched to the same publish-custody fallback contract. Hosted release workflow execution and full cross-OS signed GA artifacts remain open because Windows EV/PFX or managed-provider custody, signed Windows MSI evidence, hosted Developer ID import/HSM proof, environment protection evidence, PR review/merge, and hosted execution are still missing. | `pnpm --dir opensource/open-studio-platform verify:agent4-release-ops`; `gh release view v0.1.0 --repo auraoneai/<flagship> --json assets`; `pnpm --dir opensource/open-studio-platform run verify:release-blockers -- --probe-uptime`; `pnpm --dir opensource/open-studio-platform run verify:signing-custody`; `docs/evidence/product/open-studio-current-launch-status-2026-05-20.md`; `docs/evidence/product/open-studio-release-publish-custody-followup-2026-05-20.md`; next action: configure remaining Windows custody and run the hosted release workflow. |
| AC-21-10 | `https://updates.auraone.ai/` serves signed manifests over Cloudflare Workers. | Agent 4 | Complete for live stable macOS manifests | `updates.auraone.ai` and `updates2.auraone.ai` route to `auraone-open-update-server`; R2 serves the release GPG key, channel metadata, and signed stable v0.1.0 `latest.json`/`darwin-aarch64.json` manifests for Rubric, Robotics, and Agent. | `curl -fsS https://updates.auraone.ai/keys/auraone-open.gpg`; `curl -fsS https://updates.auraone.ai/rubric-studio-open/darwin/aarch64/0.0.0?channel=stable`; `curl -fsS https://updates.auraone.ai/robotics-studio-open/darwin/aarch64/0.0.0?channel=stable`; `curl -fsS https://updates.auraone.ai/agent-studio-open/darwin/aarch64/0.0.0?channel=stable`; `pnpm --dir opensource/open-studio-platform smoke:live`. |
| AC-21-11 | `https://intake.auraone.ai/v1/packets/` accepts valid `.auraonepkg` and returns `cloud_url`. | Agent 4 / Cloud team | Complete for current launch path | Intake Worker accepts live multipart `.auraonepkg`, validates the manifest, stores the packet in R2, returns queued `cloud_url`, and is deployed with producer binding to Cloudflare Queue `auraone-open-cloud-import` (`Number of Producers: 1`). `auraone-open-cloud-import-consumer` is deployed in production as the queue consumer (`Number of Consumers: 1`), reads the queued R2 packet, posts the packet/manifest contract to `https://auraone.ai/api/open-studio/imports`, acknowledges successful messages, retries failures, and writes audit objects to `auraone-open-cloud-import-audit`. Live smoke packet `2cdef54f-932a-4c64-91de-a8dea53db0c9` reached terminal state `imported`; audit event `open-import-2cdef54f-932a-4c64-91de-a8dea53db0c9` links packet, R2 key, Cloud project id, and Cloud project URL. | `node --test opensource/open-studio-platform/services/intake-receiver/test/intake-receiver.test.mjs`; `node --test opensource/open-studio-platform/services/cloud-import-consumer/test/cloud-import-consumer.test.mjs`; `pnpm --dir opensource/open-studio-platform run verify:cloud-import-consumer`; evidence: `docs/evidence/product/open-studio-cloud-import/`. |
| AC-21-12 | `https://install.auraone.ai/<flagship>` returns working install script. | Agent 4 | Complete for live scripts, clean-keyring hardening, and `.asc` signatures | Install Worker version `759aca6c-f51a-48d3-8fe9-6c2583a260a9` serves Rubric, Robotics, and Agent scripts with final notarized DMG filenames, Apple Silicon macOS gating, SHA-256 verification, Gatekeeper verification, release-public-key fetch/import for clean keyrings, and detached `.asc` signatures that verify against the exact live script bytes. | `curl -fsSL https://install.auraone.ai/rubric-studio-open`; `curl -fsSL https://install.auraone.ai/rubric-studio-open.asc`; `node --test opensource/open-studio-platform/services/install-server/test/install-server.test.mjs`; `docs/evidence/product/open-studio-install-server-clean-keyring-2026-05-20.md`; `pnpm --dir opensource/open-studio-platform smoke:live`. |
| AC-21-13 | Homebrew tap `auraoneai/open` exists with Rubric Studio Open cask. | Agent 4 | Complete for macOS v0.1.0 cask | `auraoneai/homebrew-open` publishes the Rubric cask with the notarized v0.1.0 Apple Silicon DMG URL, final SHA-256, and `depends_on arch: :arm64`. | `curl -fsSL https://raw.githubusercontent.com/auraoneai/homebrew-open/main/Casks/rubric-studio-open.rb`; `brew audit --cask --online --strict auraoneai/open/rubric-studio-open`. |
| AC-21-14 | winget manifest for `AuraOne.RubricStudioOpen` is published. | Agent 4 | External blocker, identity accepted | Rubric winget manifests are staged, and the Windows identity verifier now records the exact Microsoft identity/publisher, signed-MSI, and winget-manifest evidence expected for Rubric, Robotics, and Agent. Microsoft Partner Center identity and publisher-verification evidence is accepted for all three flagships; live preflight confirms Rubric x64/arm64 MSI release assets are missing and SHA/ProductCode placeholders remain; Windows identity preflight reports `readyForWindowsPackageIdentityClosure: true` and `readyForWingetSubmission: false`. | `pnpm --dir opensource/open-studio-platform run verify:winget-matrix -- --live`; `pnpm --dir opensource/open-studio-platform run verify:windows-package-identity`; next action: build EV-signed MSI artifacts, replace placeholders, submit to winget-pkgs, and verify with `winget show AuraOne.RubricStudioOpen`. |
| AC-21-15 | `docs-template/` produces Docusaurus 3 site with AuraGlass theme, nav, Algolia DocSearch, and versioned docs scaffolding. | Agent 2 | Complete for local scaffold/build; external Algolia app remains blocked | `docs-template/` builds under Docusaurus 3 with AuraGlass token CSS, canonical nav, versioned docs scaffolding, and conditional Algolia DocSearch config. | `pnpm --dir opensource/open-studio-platform/docs-template run build`; next action: provision Algolia app/indices and verify hosted DocSearch. |
| AC-21-16 | Security checklist has been run end-to-end against Rubric Studio Open GA build and third-party audit has signed off. | Agent 5 / external auditor | External blocker, 2026-05-13 | Rubric GA build and third-party audit artifact do not exist. | Next action: complete GA build, run checklist, contract auditor, attach signed audit report. |
| AC-21-17 | DCO enforcement is active on all three flagship repos and the platform repo. | Agent 5 / GitHub org admin | Public flagship enforcement complete; root external | DCO policy is documented in `CONTRIBUTING.md`; Rubric, Robotics, and Agent branch protection now require `DCO / dco` on protected PRs. `verify:dco-enforcement` records branch protection, workflow-file, and tracked public PR state. Rubric is closed for DCO enforcement; Robotics PR #33/#34/#35 and Agent PR #8/#9/#10/#11 are merged with passing `DCO / dco` checks, and the public flagship workflow-file/review gap is closed. Root branch protection remains externally unverifiable from this account even though `.github/workflows/dco.yml` exists on `main`. | `pnpm --dir opensource/open-studio-platform run verify:dco-enforcement`; `docs/evidence/product/open-studio-dco-enforcement-2026-05-20.md`; `docs/evidence/product/open-studio-current-launch-status-2026-05-20.md`; next action: provide root branch-protection evidence from an account/plan that can access the private repository protection API. If the DCO GitHub App specifically remains mandatory, attach org-admin app-install evidence. |
| AC-21-18 | `CODEOWNERS` at platform repo marks every shared file with Platform owner. | Agent 1 | Complete with org-team provisioning note | `opensource/open-studio-platform/CODEOWNERS` maps governance, templates, schemas, scripts, crates, and planned implementation surfaces. | `test -f opensource/open-studio-platform/CODEOWNERS && rg -n "@auraone/platform-owner|@auraone/security|@auraone/auraglass|@auraone/docs|/crates|/templates|/schemas|/scripts" opensource/open-studio-platform/CODEOWNERS` |
| AC-22-01 | `crates/auraone-platform-video` with GPU decode via `ffmpeg-next`. | Agent 6 | Code complete; hardware fixture execution pending | Video crate exposes host-specific hardware backend planning, software fallback, frame contracts, validation, backend-name parsing, current-host hardware backend mapping, an optional `ffmpeg` feature hook, and an ignored hardware fixture smoke test wired into the OS matrix workflow. Public Robotics CI dispatch `https://github.com/auraoneai/robotics-studio-open/actions/runs/26125939685` passed macOS arm64, Windows x64, Linux x64, and Linux arm64 hosted jobs, but the Robotics `gpu-video-embedding` job was skipped because `ROBOTICS_GPU_CI_ENABLED` is not configured and no self-hosted `linux/x64/gpu` runner is registered; no real hardware decode fixture has run. | `cargo test --workspace`; `pnpm --dir opensource/open-studio-platform run verify:code-gates`; `docs/evidence/product/robotics-studio-hosted-ci-2026-05-19.md`; next action: register an online self-hosted `linux/x64/gpu` runner, enable `ROBOTICS_GPU_CI_ENABLED=true`, and attach a passing GPU fixture run. |
| AC-22-02 | `crates/auraone-platform-dataset-stream` for chunked file IPC on >1 GB datasets. | Agent 6 | Code complete | Dataset stream crate builds manifests, redacts absolute paths, validates chunk sizes, and reads offset/length chunks with SHA-256 hashes. Unit tests verify chunk range streaming, path redaction, and a sparse 5 GiB dataset stress fixture without loading the full dataset. | `cargo test -p auraone-platform-dataset-stream`; `pnpm --dir opensource/open-studio-platform verify:extensions`; `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test --workspace`. |
| AC-22-03 | `crates/auraone-platform-ros-bag` for ROS bag adapter scaffolding. | Agent 6 | Complete for adapter scaffolding | ROS bag crate defines adapter trait, bag summary model, ROS1/ROS2 sqlite detection, metadata discovery, and tests for ROS1 `.bag` and ROS2 sqlite layouts. | `pnpm --dir opensource/open-studio-platform verify:extensions`; `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test --workspace`. |
| AC-22-04 | Intake schema v1.1 with robotics payload roles in `schemas/intake-roles.json`. | Agent 6 / Agent 3 | Complete for role registry | `schemas/intake-roles.json` exists with robotics roles introduced in `1.1.0`; `node opensource/open-studio-platform/tests/intake_roles_test.mjs` exited 0. | `node opensource/open-studio-platform/tests/intake_roles_test.mjs` |
| AC-22-05 | Sentry project for Robotics Studio Open provisioned. | Agent 6 / external Sentry admin | Complete for current launch evidence | `docs/evidence/product/open-studio-sentry-2026-05-20/` records the live `robotics-studio-open` Sentry project under org `auraone-open`, hosted `ROBOTICS_STUDIO_OPEN_SENTRY_DSN` secret availability, synthetic message event `31930598f39f4e3d93fceebbe80a2630`, synthetic crash event `26e7a83d797e4409879fba340780b76f`, and dashboard/issue-feed evidence. The observability verifier now validates the committed evidence files and reports `readyForObservabilityClosure: true` with `blockers: []` when run with the hosted-secret and uptime probes. Raw DSNs are not committed. | `pnpm --dir opensource/open-studio-platform run verify:observability -- --probe-uptime --probe-github-secrets`; `docs/evidence/product/open-studio-observability-readiness-2026-05-20.md`. |
| AC-22-06 | winget and Homebrew updated for Robotics Studio Open. | Agent 6 / Agent 4 | Homebrew complete; winget external | `auraoneai/homebrew-open` publishes the Robotics cask with the notarized v0.1.0 Apple Silicon DMG URL, final SHA-256, and `depends_on arch: :arm64`. Live winget preflight confirms x64/arm64 MSI release assets are missing and SHA/ProductCode placeholders remain. | `curl -fsSL https://raw.githubusercontent.com/auraoneai/homebrew-open/main/Casks/robotics-studio-open.rb`; `brew audit --cask --online --strict auraoneai/open/robotics-studio-open`; `pnpm --dir opensource/open-studio-platform run verify:winget-matrix -- --live`; next action: produce EV-signed MSI artifacts before winget submission. |
| AC-22-07 | Robotics Studio Open passes per-flagship security checklist. | Agent 6 / Agent 5 | Local/hosted checks pass; external signoff pending | Robotics local `verify-security-review.sh` evidence exists in the PRD, and public workflow-dispatch run `https://github.com/auraoneai/robotics-studio-open/actions/runs/26125939682` passed secret scan, Python dependency CVE scan, license scan, CycloneDX SBOM generation, artifact upload, and SBOM attestation on 2026-05-19. External reviewer signoff plus expanded fuzz campaign artifacts remain open. | `bash opensource/robotics-studio-open/scripts/verify-security-review.sh`; `docs/evidence/product/robotics-studio-hosted-ci-2026-05-19.md`; next action: attach external security reviewer signoff and expanded fuzz evidence. |
| AC-23-01 | `crates/auraone-platform-mcp` supports stdio, SSE, HTTP MCP servers. | Agent 6 | Complete for local MCP transport contract | MCP crate provides JSON-RPC request/response contracts, stdio transport, blocking HTTP transport, SSE transport, and SSE event parsing. Tests cover JSON-RPC serialization, SSE parsing, and an end-to-end local HTTP/SSE fixture that posts a request and reads a JSON-RPC SSE response event. | `pnpm --dir opensource/open-studio-platform verify:extensions`; `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test -p auraone-platform-mcp`. |
| AC-23-02 | `crates/auraone-platform-otlp` supports OTLP-proto and OTLP-JSON over gRPC/HTTP. | Agent 6 | Complete for local OTLP HTTP/gRPC receiver contract | OTLP crate validates local bind defaults, parses OTLP JSON spans, accepts HTTP OTLP JSON/proto exports, and exposes a Tonic-compatible gRPC proto acceptor with size checks. Tests cover remote-bind denial, JSON span parsing, HTTP JSON/proto export acceptance, and gRPC accept/reject behavior. | `pnpm --dir opensource/open-studio-platform verify:extensions`; `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test -p auraone-platform-otlp`. |
| AC-23-03 | `crates/auraone-platform-llm-gateway` hides BYO keys behind streaming, non-streaming, and tool-call interface. | Agent 6 | Complete for shared gateway interface contract | Gateway crate defines provider/model/message/tool contracts, streaming events, non-streaming responses, tool-call payloads, keychain-backed secret references, and request validation that rejects remote providers without a secret reference. | `pnpm --dir opensource/open-studio-platform verify:extensions`; `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test --workspace`. |
| AC-23-04 | Intake schema v1.2 with agent roles in `schemas/intake-roles.json`. | Agent 6 / Agent 3 | Complete for role registry | `schemas/intake-roles.json` exists with agent roles introduced in `1.2.0`; `node opensource/open-studio-platform/tests/intake_roles_test.mjs` exited 0. | `node opensource/open-studio-platform/tests/intake_roles_test.mjs` |
| AC-23-05 | Sentry project for Agent Studio Open provisioned. | Agent 6 / external Sentry admin | Complete for current launch evidence | `docs/evidence/product/open-studio-sentry-2026-05-20/` records the live `agent-studio-open` Sentry project under org `auraone-open`, hosted `AGENT_STUDIO_OPEN_SENTRY_DSN` secret availability, synthetic message event `a6d59cbde7c34945a1fe29f55b77442e`, synthetic crash event `3a8a12e78afc40f3a8e1966172d885c1`, and dashboard/issue-feed evidence. The observability verifier now validates the committed evidence files and reports `readyForObservabilityClosure: true` with `blockers: []` when run with the hosted-secret and uptime probes. Raw DSNs are not committed. | `pnpm --dir opensource/open-studio-platform run verify:observability -- --probe-uptime --probe-github-secrets`; `docs/evidence/product/open-studio-observability-readiness-2026-05-20.md`. |
| AC-23-06 | winget and Homebrew updated for Agent Studio Open. | Agent 6 / Agent 4 | Homebrew complete; winget external | `auraoneai/homebrew-open` publishes the Agent cask with the notarized v0.1.0 Apple Silicon DMG URL, final SHA-256, and `depends_on arch: :arm64`. Live winget preflight confirms the public x64 MSI exists and the staged `InstallerSha256` matches the GitHub release digest; EV-signing evidence is still required for winget acceptance. | `curl -fsSL https://raw.githubusercontent.com/auraoneai/homebrew-open/main/Casks/agent-studio-open.rb`; `brew audit --cask --online --strict auraoneai/open/agent-studio-open`; `pnpm --dir opensource/open-studio-platform run verify:winget-matrix -- --live`; next action: attach EV-signing evidence before winget submission. |
| AC-23-07 | Agent Studio Open passes per-flagship security checklist. | Agent 6 / Agent 5 | Pending | Agent app and checklist run do not exist in this workspace. | Next action: run checklist after Agent Studio Open build exists. |

## Non-Checkbox Requirement Coverage

| ID | PRD section | Requirement / decision | Owner | Status | Evidence | Verification |
|---|---|---|---|---|---|---|
| NC-00-01 | 0 | Path A edition vocabulary is canonical: Open, Cloud, Enterprise, Programs. | Agent 1 / all flagships | Complete for PRD alignment | Cross-PRDs reference Open/Cloud/Enterprise/Programs; alignment notes added in PRD status block. | `rg -n "Open Studio Platform|Open|Cloud|Enterprise|Programs" docs/prds/*open-prd.md` |
| NC-00-02 | 0 | Vendor swaps require platform-level RFC. | Agent 1 | Complete | `RFC_PROCESS.md`, `GOVERNANCE.md`, `rfcs/0000-template.md`. | `rg -n "Vendor|RFC|required|vendor" opensource/open-studio-platform/RFC_PROCESS.md opensource/open-studio-platform/GOVERNANCE.md` |
| NC-01-01 | 1.1 | Platform owns shared substrate: Tauri template, AuraGlass IDE kit, signing, updater, crash, telemetry, keychain, intake, installer, docs, security, license, contributor model. | Agents 1-6 | Partially complete | Governance root exists; implementation surfaces are mapped and mostly pending. | `find opensource/open-studio-platform -maxdepth 3 -type f | sort` |
| NC-01-02 | 1.3 | All three flagships use the same shared contracts. | Agents 1-6 | Code complete | Rubric, Robotics, and Agent declare local file dependencies on `@auraone/platform-contracts` and `@auraone/aura-ide-kit`, import the generated shared contract artifacts, and have a regression test proving actual source consumption. | `node --test opensource/open-studio-platform/tests/flagship_integration_contracts_test.mjs` |
| NC-01-03 | 1.4 | AuraGlass source of truth is external; no `opensource/auraglass` directory should be referenced. | Agent 1 / Agent 2 | Complete for PRD alignment | Platform PRD and Rubric PRD name `auraglasswebsite` / AuraGlass URLs. | `! rg -n "opensource/auraglass" docs/prds/*.md opensource/open-studio-platform` |
| NC-03-01 | 3 | Scope split table: platform concerns remain platform-owned, flagship product screens and engine integrations remain flagship-owned. | Agent 1 / all agents | Complete for governance docs; pending implementation | `GOVERNANCE.md`, `CODEOWNERS`, matrix owners. | `rg -n "Platform owner|Flagship engineering lead|CODEOWNERS|flagship" opensource/open-studio-platform/GOVERNANCE.md opensource/open-studio-platform/CODEOWNERS` |
| NC-03-02 | 3 | GitHub issue/PR templates live under platform `.github-templates/`. | Agent 1 | Complete | `.github-templates/PULL_REQUEST_TEMPLATE.md` and issue templates. | `find opensource/open-studio-platform/.github-templates -type f | sort` |
| NC-04-01 | 4.1 | Version pins: Tauri 2.x, Rust exact via toolchain, Node 20.19.5, pnpm 8.15.8+, React 18.x, TS 5.4+ strict, Vite 5.x, Python 3.11+. | Agent 2 | Code complete for local pins | Template and workspace manifests pin Tauri 2.11.x, Node >=20.19.5, React 18.3.x, TypeScript 5.9.x, Vite 5.4.x, strict TS builds pass, and Agent/Robotics/Rubric Python sidecar/reservation packages now require Python `>=3.11` with no Python 3.10 classifier. | `node --test opensource/open-studio-platform/tests/flagship_integration_contracts_test.mjs`; `pnpm --dir opensource/rubric-studio-open run typecheck`; `pnpm --dir opensource/robotics-studio run typecheck`; `pnpm --dir opensource/agent-studio-open run typecheck` |
| NC-04-02 | 4.4 | IPC default is subprocess sidecars; PyO3 only for latency-sensitive paths >50 ms threshold. | Agent 2 / Agent 3 | Complete for shared sidecar contract | `auraone-platform-sidecar` provides the JSON-line subprocess envelope, timeout handling, output limits, and child-crash isolation; template docs define the PyO3 exception threshold at measured >50 ms interactive calls and require security-review documentation. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test -p auraone-platform-sidecar`; `node opensource/open-studio-platform/scripts/verify-platform-contracts.mjs`. |
| NC-04-03 | 4.5 | Every flagship is single-window with command palette and multi-tab; standard `registerCommand` API. | Agent 2 | Complete for shared package/template contract | Shell template consumes `AuraIdeAppFrame`, tabs, command palette, and `createDefaultIdeCommands`; package tests cover command registration/run behavior. | `pnpm --dir opensource/open-studio-platform test`; `pnpm --dir opensource/open-studio-platform/templates/tauri-app run verify`. |
| NC-04-04 | 4.6 | Project-as-folder convention and five open modes: file picker, drag/drop, deep link, recent projects, CLI. | Agent 2 | Complete for local template contract | Template creates `.auraone/project.json`, records recent projects, supports file-picker open, drag/drop folder open via Tauri webview events, deep-link `open-project?path=...`, recent project commands, and CLI folder arguments. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test --manifest-path opensource/open-studio-platform/templates/tauri-app/src-tauri/Cargo.toml`; `pnpm --dir opensource/open-studio-platform/templates/tauri-app run typecheck`; `node opensource/open-studio-platform/scripts/verify-platform-contracts.mjs`. |
| NC-04-05 | 4.7 | Single `auraone://` scheme routed by first path segment; fallback to install page if target flagship is absent. | Agent 2 / Agent 4 | Complete for local deep-link contract | Deep-link parser routes by first path segment, emits action/params to the renderer, and includes `https://auraone.ai/open/<flagship>` fallback metadata for unknown flagships; renderer opens the fallback install page. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test --manifest-path opensource/open-studio-platform/templates/tauri-app/src-tauri/Cargo.toml`; `node opensource/open-studio-platform/scripts/verify-platform-contracts.mjs`. |
| NC-04-06 | 4.8 | CSP connect-src, updater endpoint, pubkey, deep-link scheme are platform-owned and non-negotiable. | Agent 2 / Agent 4 / Agent 5 | Complete for template contract | Platform contract verifier checks canonical CSP, `auraone://`, update endpoints, and disallowed capabilities against `templates/tauri-app/src-tauri/tauri.conf.json` and `capabilities/default.json`. | `node opensource/open-studio-platform/scripts/verify-platform-contracts.mjs`. |
| NC-04-07 | 4.9 | Default Tauri capabilities are restricted; `shell:open`, `shell:execute`, arbitrary `http:` are disabled by default; exceptions require CODEOWNERS review. | Agent 2 / Agent 5 | Complete for local template and review contract | Template `capabilities/default.json` is an explicit allowlist and excludes shell/http capabilities and remote URLs; platform contract verification fails on any default `shell:*`, `http:*`, or remote capability URL. PR template flags Tauri CSP/ACL changes for review. | `node opensource/open-studio-platform/scripts/verify-platform-contracts.mjs`; `rg -n "Tauri|ACL|CSP|network" opensource/open-studio-platform/.github-templates/PULL_REQUEST_TEMPLATE.md`. |
| NC-04-08 | 4.10 | Build matrix covers macOS Intel/Apple Silicon/universal, Windows x86_64/aarch64, Linux x86_64/aarch64 with specified formats. | Agent 4 | Present and linted; actual OS matrix builds external to CI runner availability | Release workflow templates include macOS, Windows, and Linux matrix entries, and `actionlint` passes. Actual signed OS builds still require GitHub runners, signing secrets, and release artifacts. | `go run github.com/rhysd/actionlint/cmd/actionlint@latest opensource/open-studio-platform/.github-templates/workflows/release.yml`; next action: run staging workflow on GitHub after signing secrets exist. |
| NC-05-01 | 5.1 | AuraGlass canonical locations are live site, playground, GitHub, local source, npm `aura-glass`; no `opensource/auraglass`. | Agent 1 / Agent 2 | Complete for docs | Platform PRD and Rubric PRD carry canonical references. | `rg -n "auraglass.auraone.ai|aura-glass|auraglasswebsite|opensource/auraglass" docs/prds/open-studio-platform-prd.md docs/prds/rubric-studio-open-prd.md` |
| NC-05-02 | 5.2 | Required IDE components: command palette, sidebar, tree, tabbed editor, bottom panel, toast, modal, form inputs, Monaco, shortcut surface, themes, status bar, project row, file watcher, welcome window. | Agent 2 | Complete for shared IDE kit component surface | `packages/aura-ide-kit/` builds and component/command-registry tests pass for the shared frame, tree, command palette, settings/privacy, status bar, tabbed shell, Monaco surface, file watcher status, welcome window, intake preview, problems panel, and status bar. | `pnpm --dir opensource/open-studio-platform/packages/aura-ide-kit test`; `pnpm --dir opensource/open-studio-platform/packages/aura-ide-kit build`. |
| NC-05-03 | 5.3 | Contribution-back procedure: incubate in `packages/aura-ide-kit`, upstream after two flagships/no churn for two releases, deprecate local copy. | Agent 1 / Agent 2 | Complete for process; pending implementation | `GOVERNANCE.md` and PRD retain procedure. | `rg -n "AuraGlass|aura-ide-kit|upstream" opensource/open-studio-platform/GOVERNANCE.md docs/prds/open-studio-platform-prd.md` |
| NC-05-04 | 5.4 | New IDE components declare SSR posture: `ssr-safe`, `client-only`, or `client-only-with-suspense`. | Agent 2 | Complete for exported IDE kit components | `src/ssr-posture.ts` declares posture and rationale for every exported IDE component, and Vitest verifies the manifest covers the exported surface. | `pnpm --dir opensource/open-studio-platform/packages/aura-ide-kit test`. |
| NC-05-05 | 5.5 | IDE components meet WCAG 2.1 AA and axe-core blocks failures. | Agent 2 / Agent 5 | Complete for local shared-shell axe gate | Aura IDE kit includes an axe-core Vitest check against the shared shell composition, including tree, tabs, intake packet preview, problems panel, status bar, and command-palette affordance. | `pnpm --dir opensource/open-studio-platform/packages/aura-ide-kit test`. |
| NC-06-01 | 6.1 | One certificate set covers all three flagships: Apple Developer ID app/installer, Windows EV or managed signing, Linux GPG, Tauri updater Ed25519. | Agent 4 / external cert admin | Partial: Apple macOS v0.1.0 and local GPG signatures complete; Windows custody pending | AuraOne Open release GPG fingerprint `F909 806D 13D9 CD4C F403 FA3C 8C61 E177 EB63 29E7` is published through `updates.auraone.ai`; updater public key `DAKD/Nqj4KoXZpXv9li/zVQv+2LhThXE5J9tx0Wl1B8=` is wired into platform/Rubric config. Apple Developer ID signing/notarization passed locally for all three macOS Apple Silicon DMGs using Team ID `6T44QQW78K`. Current checksum and installer-script detached GPG signatures are published. The Windows signer now supports certificate thumbprint, PFX, or Microsoft Azure Artifact Signing/Trusted Signing via `signtool /dlib`; actual EV/managed signing account custody remains external. | `curl -fsS https://updates.auraone.ai/keys/auraone-open.gpg`; `xcrun stapler validate`; `spctl -a -vvv -t install`; `pnpm --dir opensource/open-studio-platform smoke:live`; next action: provision Windows EV cert, PFX, or managed Azure Artifact Signing custody and run signed canary verification. |
| NC-06-02 | 6.2 | macOS pipeline signs nested binaries, hardened runtime, timestamps, notarizes/staples `.app` and `.dmg`. | Agent 4 | Complete for local v0.1.0 Apple Silicon DMGs; hosted CI workflow pending | `sign-macos.sh`, `notarize.sh`, and `build-signed-macos-flagships.sh` exist and shell syntax passed. Local Apple Silicon `.app` and `.dmg` artifacts for Rubric Studio Open, Robotics Studio Open, and Agent Studio Open were signed with `Developer ID Application: Gurbaksh Chahal (6T44QQW78K)`, accepted by Apple notarization, stapled, validated with `xcrun stapler validate`, and accepted by `spctl -a -vvv -t install` with `source=Notarized Developer ID`. | `xcrun stapler validate`; `spctl -a -vvv -t install`; next action: move signing/notary custody into CI/HSM and run the hosted release workflow. |
| NC-06-03 | 6.3 | Windows pipeline signs MSI via WiX 4 and signtool with timestamp server. | Agent 4 | Code syntax verified; external signing blocker remains | `sign-windows.ps1` exists and parses with local `pwsh` 7.6.1. The signer accepts a Windows certificate thumbprint, PFX/password pair, or `AURAONE_WINDOWS_SIGNING_PROVIDER=azure-artifact-signing` with Azure Artifact Signing DLIB plus metadata or endpoint/account/profile secrets; dry-run coverage exists for thumbprint and managed-provider command rendering. EV cert/HSM/managed account custody and signed MSI canary remain external/account-gated release evidence. | `node opensource/open-studio-platform/scripts/verify-powershell-syntax.mjs`; next action: run signed MSI canary after cert or managed signing provisioning. |
| NC-06-04 | 6.4 | Linux pipeline signs AppImage, deb, rpm, tarballs and publishes GPG key. | Agent 4 | Complete for current v0.1.0 Linux artifacts; hosted workflow reproduction pending | `sign-linux.sh` exists and shell syntax passed; release GPG public key/fingerprint are published, and current v0.1.0 checksum sets have `SHA256SUMS.asc`. AppImage/deb/rpm metadata is prepared for all three flagships. The 2026-05-20 Linux artifact preflight confirms Rubric, Robotics, and Agent have live signed AppImage/deb/rpm assets for the current release matrix with detached `.asc` signatures, `.sha256` files, signed package-repo metadata, signature verification, and Ubuntu/Fedora clean-install evidence under `docs/evidence/product/linux-artifacts/`. Remaining Linux work is hosted release-workflow reproduction/audit evidence, not missing public package assets. | `curl -fsS https://updates.auraone.ai/keys/auraone-open.gpg`; `pnpm --dir opensource/open-studio-platform run verify:linux-package-matrix`; `pnpm --dir opensource/open-studio-platform run verify:linux-artifacts`; next action: rerun from the protected hosted release workflow and record audit evidence. |
| NC-06-05 | 6.5 | HSM signing procedure: request queue, artifact hash verification, owner/back-up, 15-minute CI wait, revocation/loss procedure. | Agent 4 / external ops | Complete for local runbook contract; HSM provisioning external | `security/runbooks/hsm-signing-operations.md` documents the private signing request queue, artifact hash verification against CI and queue hashes, primary/backup owners, 15-minute CI fail-closed timeout, two-person controls, storage/backup, audit evidence, and failure handling. `security/runbooks/signing-key-incident-response.md` documents revocation/loss procedure. Real HSM, queue, and cert provisioning remain external blockers. | `node --test opensource/open-studio-platform/scripts/test/signing-runbooks.test.mjs`; next action: provision the HSM-backed queue and run a signed canary. |
| NC-06-06 | 6.6 | Flagships must call only platform signing wrappers; CI lint blocks copied signing scripts. | Agent 4 / Agent 5 | Complete for local CI policy | CODEOWNERS maps signing scripts and `scripts/check-signing-wrapper-usage.mjs` blocks copied signing scripts or workflow references that bypass `opensource/open-studio-platform/scripts/*`; security workflow template runs the verifier. | `node opensource/open-studio-platform/scripts/check-signing-wrapper-usage.mjs .`; `go run github.com/rhysd/actionlint/cmd/actionlint@latest opensource/open-studio-platform/.github-templates/workflows/security.yml`. |
| NC-07-01 | 7.1 | Update architecture uses CI, R2 bucket `auraone-open-updates`, Workers endpoint, signed manifests, installer re-verification, AuraGlass prompt. | Agent 4 / Agent 2 | Complete for live stable macOS manifests; CI release execution pending | Update Worker, R2 objects, signed manifests, channel metadata, release key route, and fallback host are live for Rubric Studio Open, Robotics Studio Open, and Agent Studio Open stable v0.1.0 macOS manifests. The hosted CI release workflow still needs to produce the full cross-OS production artifact set. | `pnpm --dir opensource/open-studio-platform smoke:live`; next action: run hosted release workflow once cert secrets exist. |
| NC-07-02 | 7.2 | Channels: stable monthly default, beta weekly opt-in, nightly every green main opt-in. | Agent 4 / Agent 2 | Complete for local update Worker contract | Update Worker fallback channel metadata encodes stable as monthly/default/not opt-in, beta as weekly/opt-in, and nightly as every-green-main/opt-in; tests verify all three channels and unknown-channel rejection. | `node --test opensource/open-studio-platform/services/update-server/test/update-server.test.mjs`; next action: deploy and verify the same metadata at `updates.auraone.ai`. |
| NC-07-03 | 7.3 | Rollout manifest includes percentage, mandatory, min_version; kill switch is percentage 0 plus mandatory min_version bump. | Agent 3 / Agent 4 | Complete for local manifest and kill-switch contract | Updater crate tests and release-ops verifier cover rollout percentage, mandatory/min-version fields, deterministic rollout decisions, signed manifest generation, and kill-switch no-update behavior. | `pnpm --dir opensource/open-studio-platform verify:agent4-release-ops`; `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test --workspace`. |
| NC-07-04 | 7.4 | Hosting is locked to Cloudflare R2 + Workers unless RFC changes it. | Agent 1 / Agent 4 | Complete for governance; external implementation pending | RFC process requires vendor RFC. | `rg -n "vendor|Cloudflare|RFC" opensource/open-studio-platform/RFC_PROCESS.md docs/prds/open-studio-platform-prd.md` |
| NC-07-05 | 7.5 | Update notification UX has release notes, install next launch/default, install now, remind later, version display, signed-by tooltip; mandatory updates skip remind. | Agent 2 / Agent 3 | Complete for shared update prompt contract | `AuraUpdatePrompt` renders version, release notes, signed-by tooltip, install next launch/default, install now, and remind later; component tests verify mandatory updates disable the remind-later action. | `pnpm --dir opensource/open-studio-platform/packages/aura-ide-kit test`; next action: wire and verify the shared prompt in each flagship update flow. |
| NC-07-06 | 7.6 | Updater verifies manifest signature before download and surfaces clear error on failure. | Agent 3 | Complete for local verifier behavior | Updater tests verify valid signature acceptance, tampered manifest rejection, and user-visible verification failure messaging. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test --workspace`. |
| NC-08-01 | 8.1 | Crash reporter vendor is Sentry with self-hosted endpoint override. | Agent 3 / Agent 5 | Complete for local crash reporter contract and current Sentry provisioning | Crash crate and Sentry config exist; tests verify Sentry posture and opt-in/default-off behavior. Live Sentry project, hosted DSN secret, synthetic upload, dashboard, and Rubric uptime evidence is recorded for the current Rubric, Robotics, and Agent launch set. Browser-created evidence is accepted; the Sentry API probe is optional and requires `SENTRY_AUTH_TOKEN` only when `--probe-sentry` is requested. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test --workspace`; `pnpm --dir opensource/open-studio-platform security:all`; `pnpm --dir opensource/open-studio-platform run verify:observability -- --probe-uptime --probe-github-secrets`. |
| NC-08-02 | 8.2 | Crash reporting is off by default; welcome wizard copy/options match PRD. | Agent 2 / Agent 3 | Complete for shared welcome wizard contract | `AuraWelcomePrivacyWizard` renders default-off crash reporting, explicit crash opt-in copy, and PRD disallowed-content copy; component tests verify the crash checkbox starts unchecked. | `pnpm --dir opensource/open-studio-platform/packages/aura-ide-kit test`; next action: wire and verify the shared wizard in each flagship first-run flow. |
| NC-08-03 | 8.3 | Crash scrubber removes home paths, hostname, username, API keys, project paths, HTTP bodies/query/headers, env vars. | Agent 3 / Agent 5 | Complete for local scrub fixtures | Crash tests verify path/API-key scrubbing and security config checks validate scrub config artifacts. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test --workspace`; `pnpm --dir opensource/open-studio-platform security:all`. |
| NC-08-04 | 8.4 | Minidumps via `minidump-writer`, temp storage, scrubbed, uploaded next launch when opted in. | Agent 3 | Complete for local minidump lifecycle contract; native crash fixture pending | Crash crate ships `minidump-writer`, writes sanitized `.dmp` files to a `MinidumpStore`, keeps pending dumps local while crash reporting is disabled, and uploads/removes pending dumps on next launch after opt-in. Real OS native crash capture still requires supported-runner fixtures. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test -p auraone-platform-crash`; next action: run native crash fixture tests on macOS, Windows, and Linux runners. |
| NC-08-05 | 8.5 | Crash sample rate is 100% after opt-in. | Agent 3 / Agent 5 | Complete for local crash contract | Rust crash reporter reports every enabled crash event and exposes an effective sample rate of 1.0 after opt-in; TypeScript platform contracts expose `sampleRate: 1` in the default Sentry config. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test -p auraone-platform-crash`; `pnpm --dir opensource/open-studio-platform/packages/platform-contracts build && node opensource/open-studio-platform/tests/platform-contracts.test.mjs`. |
| NC-09-01 | 9.1 | Telemetry opt-in only, default off, exact consent copy and no content collection. | Agent 2 / Agent 3 / Agent 5 | Complete for shared consent UI and local validation | Telemetry crate/schema/contracts enforce forbidden content, and `AuraWelcomePrivacyWizard` renders opt-in telemetry unchecked by default with explicit no-content/no-key/no-path/no-hostname copy. | `pnpm --dir opensource/open-studio-platform/packages/aura-ide-kit test`; `pnpm --dir opensource/open-studio-platform security:all`; next action: wire and verify the shared wizard in each flagship first-run flow. |
| NC-09-02 | 9.2 | `schemas/telemetry.schema.json` enforces canonical event shape. | Agent 3 / Agent 5 | Complete for local schema/validator fixtures | Telemetry schema, platform contract verifier, telemetry crate tests, and node contract tests pass for canonical event structure. | `pnpm --dir opensource/open-studio-platform verify`; `pnpm --dir opensource/open-studio-platform test`. |
| NC-09-03 | 9.3 | `schemas/telemetry-events.json` registry includes name, description, payload schema, owner, since; platform owner reviews additions. | Agent 3 / Agent 5 | Complete for local registry contract and public CODEOWNERS enforcement | Event registry exists and contract tests verify platform events consumed by Rubric Studio Open. Public Rubric, Robotics, and Agent repositories now require two approving reviews plus CODEOWNERS review after the 2026-05-19/2026-05-20 cross-repo enforcement pass. Root private-repository branch-protection verification remains inaccessible from this account because GitHub returns the private-repository plan/API limitation. | `pnpm --dir opensource/open-studio-platform verify`; `pnpm --dir opensource/open-studio-platform run verify:dco-enforcement`; next action: verify root private-repository branch protection with an account/plan that can read the API. |
| NC-09-04 | 9.4 | Telemetry forbids content, API keys, paths, hostnames/IPs, free text, names/emails; schema rejects suspicious fields. | Agent 3 / Agent 5 | Complete for local forbidden-field checks | Telemetry crate tests and security scanners verify forbidden-field scrubbing/rejection. | `pnpm --dir opensource/open-studio-platform security:all`; `pnpm --dir opensource/open-studio-platform test`. |
| NC-09-05 | 9.5 | Transport uses `https://o.auraone.ai/v1/events`, batches 100 events/30 sec, fails silently. | Agent 3 / Agent 4 | Complete for live Worker endpoint | Telemetry Worker validates registered events, rejects forbidden fields, stores accepted batches in R2, and is routed at `o.auraone.ai/*`. Public DNS and Cloudflare IP-pinned health/upload checks pass; the full live smoke also passes with `AURAONE_TELEMETRY_RESOLVE_IP` while local macOS resolver propagation catches up. | `AURAONE_TELEMETRY_RESOLVE_IP=104.21.10.111 pnpm --dir opensource/open-studio-platform smoke:live`; next action: rerun normal `pnpm --dir opensource/open-studio-platform smoke:live` after local resolver cache expires. |
| NC-09-06 | 9.6 | User-visible telemetry event log shows local-preview/would-send JSON, exports, and clears the local queue when disabled. | Agent 2 / Agent 3 | Code complete; GA screenshot evidence pending | Telemetry contract tests prove that opt-in records remain local previews until a real transport confirms delivery, while disabled telemetry records would-send previews; package/component tests cover the accessible settings/privacy surface; Rubric, Robotics, and Agent settings screens render the shared `AuraTelemetryEventLog`. | `pnpm --dir opensource/open-studio-platform run verify:code-gates`; `node --test opensource/open-studio-platform/tests/flagship_integration_contracts_test.mjs`; next action: capture release UI evidence after GA builds exist. |
| NC-09-07 | 9.7 | Published schema URL is authoritative and versioned with previous major read support. | Agent 3 / Agent 4 | Complete for v1 schema publication | Cloudflare Pages serves flat and versioned telemetry/intake schemas, including `/open-studio/telemetry/v1.json` and `/open-studio/intake-packet/v1.json`. | `curl -fsSI https://schemas.auraone.ai/open-studio/telemetry/v1.json`; `curl -fsSI https://schemas.auraone.ai/open-studio/intake-packet/v1.json`. |
| NC-10-01 | 10.1 | Keychain backends: macOS `security-framework`, Windows `windows-credentials`, Linux `secret-service`, Linux AES-256-GCM fallback. | Agent 3 | Partial: native provider wired; real native backend smoke tests pending | Keychain crate includes encrypted file fallback, backend-kind contracts, and target-specific `keyring` 3.6.3 providers for macOS Keychain, Windows Credential Manager, and Linux Secret Service. Tests verify encrypted fallback and shared API behavior; local Linux can fall back to the encrypted file backend if Secret Service is unavailable. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test --workspace`; next action: run real OS native backend smoke tests. |
| NC-10-02 | 10.2 | Rust and TypeScript keychain API surfaces mirror set/get/delete/list and IPC redacts secrets. | Agent 3 / Agent 2 | Complete for Rust API and platform contract; generated TS package wiring pending publish | Rust crate tests verify set/get/delete/list and secret redaction; node contract tests verify keychain API routing through shared Tauri IPC commands with secret suppression. | `pnpm --dir opensource/open-studio-platform test`. |
| NC-10-03 | 10.3 | Keychain stores BYO API keys, Sentry token, Hugging Face token, AuraOne Cloud token, not user content. | Agent 3 / Agent 5 | Complete for local scope policy | Rust and TypeScript keychain validators allow only approved secret scopes and reject `project-content` before storing or invoking IPC. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test -p auraone-platform-keychain`; `pnpm --dir opensource/open-studio-platform/packages/platform-contracts build && node opensource/open-studio-platform/tests/platform-contracts.test.mjs`. |
| NC-10-04 | 10.4 | Encrypted-at-rest guarantee per backend; Linux fallback warning on first set. | Agent 3 / Agent 2 | Complete for local backend and warning UI contract; real OS encryption smoke pending | macOS and Windows use native OS stores without silent downgrade; Linux Secret Service falls back to the AES-256-GCM file backend when the service is unavailable. `AuraKeychainFallbackWarning` renders the first-secret Linux fallback warning and stays hidden for native Secret Service. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test -p auraone-platform-keychain`; `pnpm --dir opensource/open-studio-platform/packages/aura-ide-kit test`; next action: run native encryption smoke tests on macOS, Windows, and Linux Secret Service. |
| NC-10-05 | 10.5 | API keys never logged to crash, telemetry, stack traces, console, IPC logs. | Agent 3 / Agent 5 | Complete for shared local diagnostics contract | Crash and telemetry tests scrub API-key-like values; `SecretString` debug redacts; platform contract tests mark keychain IPC calls as secret, reject user-content scopes before invoke, and sanitize console args/Error stack traces before diagnostics logging. | `pnpm --dir opensource/open-studio-platform/packages/platform-contracts build && node opensource/open-studio-platform/tests/platform-contracts.test.mjs`; next action: verify each flagship routes renderer diagnostics through the shared sanitizer. |
| NC-11-01 | 11.3 | `.auraonepkg` is ZIP with `manifest.json`, `payload/`, `signatures/manifest.sig`; install keypair stored in keychain. | Agent 3 | Code complete; native OS keychain smoke evidence pending | Intake crate tests verify ZIP layout, manifest generation, payload entries, and signature file; shared TypeScript and Rust keychain scopes include `intake-install-signing-key`; Rubric, Robotics, and Agent shell/UI flows call the shared install-key persistence helper before intake export/signing flows. | `pnpm --dir opensource/open-studio-platform run verify:code-gates`; `node --test opensource/open-studio-platform/tests/platform-contracts.test.mjs opensource/open-studio-platform/tests/flagship_integration_contracts_test.mjs`; next action: run native OS keychain smoke tests on GA runners. |
| NC-11-02 | 11.4 | Manifest required fields and user-supplied display/email/intent must not auto-read OS/git identity. | Agent 3 / Agent 2 / Agent 5 | Complete for shared manifest and UI provenance contract | Intake schema/contracts require display name and intent, and `AuraIntakeIdentityFields` renders controlled display/email/intent inputs with empty caller-supplied defaults; component tests verify values only change through user input callbacks. | `pnpm --dir opensource/open-studio-platform/packages/aura-ide-kit test`; `pnpm --dir opensource/open-studio-platform/packages/platform-contracts build && node opensource/open-studio-platform/tests/platform-contracts.test.mjs`; next action: wire the shared identity form in each flagship intake dialog. |
| NC-11-03 | 11.5 | Payload roles are canonical for Rubric, Robotics, Agent in `schemas/intake-roles.json`. | Agent 3 / Agent 6 | Complete for role registry | `schemas/intake-roles.json` exists and `node opensource/open-studio-platform/tests/intake_roles_test.mjs` exited 0. | `node opensource/open-studio-platform/tests/intake_roles_test.mjs` |
| NC-11-04 | 11.6 | `.auraonepkg` never contains API keys, raw robotics video, unscrubbed agent trace payload secrets, implicit PII, file paths, hostnames, env vars. | Agent 3 / Agent 5 / Agent 6 | Complete for local packet builder privacy gate; flagship content audits pending | Intake packet builder rejects API-key markers, local path markers, hostname/private-key markers, and embedded raw robotics media before preview/write; TypeScript manifest contracts reject raw robotics media paths. | `CARGO_TARGET_DIR=/tmp/auraone-open-studio-target cargo test -p auraone-platform-intake`; `pnpm --dir opensource/open-studio-platform/packages/platform-contracts build && node opensource/open-studio-platform/tests/platform-contracts.test.mjs`; next action: audit each flagship exporter against the shared packet builder. |
| NC-11-05 | 11.7 | Mandatory local preview shows summary, file-by-file preview, manifest tree, exact privacy copy, required consent checkbox. | Agent 2 / Agent 3 | Complete for shared preview component contract | `AuraIntakePacketPreview` renders packet summary, file-by-file preview, excluded patterns, optional manifest tree, verbatim PRD privacy copy, the canonical privacy link, and a required consent checkbox; component tests verify the preview and consent callback. | `pnpm --dir opensource/open-studio-platform/packages/aura-ide-kit test`; next action: wire and verify the shared preview in each flagship intake flow. |
| NC-11-06 | 11.8 | Transport is multipart POST to `https://intake.auraone.ai/v1/packets/`, supports optional Enterprise mTLS. | Agent 3 / Agent 4 | Complete for public Worker upload path; Enterprise mTLS pending | Live intake Worker accepts multipart packet upload and returns queued import metadata. Optional Enterprise mTLS was not exercised in this public launch smoke. | `pnpm --dir opensource/open-studio-platform smoke:live`; next action: add Enterprise mTLS staging check when Enterprise endpoint exists. |
| NC-11-07 | 11.9 | Cloud import accepts last three schema majors, validates manifest, rejects structured errors, surfaces `cloud_url`, sends magic link if email provided. | Cloud team / Agent 4 | Current live import path complete; compatibility/magic-link matrix pending | The deployed intake plus queue consumer path validates the current v1 packet contract, posts the packet/manifest payload to the live Cloud import API, surfaces an imported `cloud_url`, and writes audit linkage. The broader last-three-major compatibility matrix, structured rejection body probes, and optional magic-link email delivery still need staging/live evidence. | Next action: run staging contract tests for accepted schema majors, structured error rejection, `cloud_url`, and optional magic-link email. |
| NC-11-08 | 11.10 | Failure modes: network outbox/backoff, 422 diagnostics, size limit, 409 version mismatch, auth after import. | Agent 3 / Agent 4 | Complete for local receiver/client contracts | Platform contracts classify network/server failures into local outbox retry with capped exponential backoff; intake receiver tests cover 413 size limit, 422 diagnostics, 409 future manifest version mismatch, and success responses that hand off auth after import via `cloud_url`/`next_step`. | `pnpm --dir opensource/open-studio-platform/packages/platform-contracts build && node opensource/open-studio-platform/tests/platform-contracts.test.mjs`; `node --test opensource/open-studio-platform/services/intake-receiver/test/intake-receiver.test.mjs`; next action: verify against deployed staging intake Worker. |
| NC-11-09 | 11.11 | Privacy copy is verbatim and links to `https://auraone.ai/open/privacy/intake`. | Agent 2 / Agent 4 | Complete for shared local copy; public URL external blocker | `AuraIntakePacketPreview` exports/reuses the verbatim PRD privacy copy and links to `https://auraone.ai/open/privacy/intake`; production URL publication still requires docs deployment. | `pnpm --dir opensource/open-studio-platform/packages/aura-ide-kit test`; next action: publish the public privacy page and verify the live URL. |
| NC-12-01 | 12.1 | Installer distribution formats: macOS signed/notarized DMG, Windows signed MSI/EXE, Linux signed AppImage/deb/rpm. | Agent 4 | macOS and Linux v0.1.0 complete; Windows pending | Release workflow, scripts, and installer/distribution scaffolds exist. Local Apple Silicon DMGs for Rubric, Robotics, and Agent are Developer ID signed, Apple notarized, stapled, Gatekeeper-accepted, and published on GitHub Releases with fresh `SHA256SUMS` assets. Linux AppImage/deb/rpm assets are signed, published, and evidenced for the current Rubric, Robotics, and Agent release matrix. Windows signed MSI/EXE artifacts and hosted release workflow execution remain pending. | `pnpm --dir opensource/open-studio-platform run verify:linux-artifacts`; next action: provision Windows EV/HSM/PFX or managed Azure Artifact Signing custody and rerun the protected hosted release workflow. |
| NC-12-02 | 12.2 | Homebrew tap `auraoneai/open` and casks for all three flagships. | Agent 4 | Complete for macOS v0.1.0 casks | `auraoneai/homebrew-open` publishes Rubric, Robotics, and Agent casks pointing at the notarized v0.1.0 Apple Silicon DMGs and published SHA-256 values. Homebrew audit passed against the published tap. | `brew audit --cask --online --strict auraoneai/open/rubric-studio-open auraoneai/open/robotics-studio-open auraoneai/open/agent-studio-open`. |
| NC-12-03 | 12.3 | winget publisher `AuraOne` and manifests for all three flagships. | Agent 4 | External blocker, live preflight refreshed 2026-05-19 | All three manifest sets are staged. Rubric/Robotics are blocked on missing MSI assets plus SHA/ProductCode placeholders. Agent has a live MSI and matching SHA but no EV-signing evidence. | `pnpm --dir opensource/open-studio-platform run verify:winget-matrix -- --live`; next action: complete EV-signed MSI evidence and submit manifests after placeholders are replaced. |
| NC-12-04 | 12.4 | Install script detects OS/arch, downloads signed artifact, verifies signature/notarization, installs, prints fingerprint/docs URL; script is GPG-signed. | Agent 4 | Complete for live scripts and GPG script signatures | Install Worker serves Rubric, Robotics, and Agent scripts. Shared installers resolve final notarized macOS DMG filenames, enforce Apple Silicon for macOS channels, verify Gatekeeper by default, and publish detached `.asc` signatures for each shell installer. | `curl -fsSL https://install.auraone.ai/rubric-studio-open`; `curl -fsSL https://install.auraone.ai/rubric-studio-open.asc`; `pnpm --dir opensource/open-studio-platform smoke:live`. |
| NC-12-05 | 12.5 | Release workflow triggers on `v*`, builds matrix, signs, generates updater manifest, uploads GitHub Releases/R2, bumps Homebrew/winget. | Agent 4 | Present and linted; external publish steps blocked | Release workflow exists, triggers on `v*`, runs a three-OS matrix, signs per platform, generates updater manifests, publishes GitHub/R2 artifacts, and requests Homebrew/winget bumps. `actionlint` and local release-ops verifier pass; real publish requires credentials and signed artifacts. | `go run github.com/rhysd/actionlint/cmd/actionlint@latest opensource/open-studio-platform/.github-templates/workflows/release.yml`; `pnpm --dir opensource/open-studio-platform verify:agent4-release-ops`; next action: run a staging release with credentials. |
| NC-12-06 | 12.6 | Every artifact has `SHA256SUMS` and `SHA256SUMS.asc`; docs include verify page. | Agent 4 / Agent 2 | Complete for current v0.1.0 GitHub Release checksum sets; full GA matrix pending | Rubric, Robotics, and Agent `0.1.0` release directories include `SHA256SUMS` and `SHA256SUMS.asc`, and the signed checksum assets are attached to the GitHub Releases. Full Windows/Linux GA artifact checksum coverage remains pending. | Local files under `distribution/releases/*/0.1.0`; `gh release view v0.1.0 --repo auraoneai/<flagship> --json assets`; next action: generate signed checksum sets for every future GA artifact. |
| NC-13-01 | 13.1 | Docs generator is Docusaurus 3. | Agent 2 | Complete for local scaffold/build | `docs-template/package.json` pins Docusaurus 3 and the template builds successfully. | `pnpm --dir opensource/open-studio-platform/docs-template run build`. |
| NC-13-02 | 13.2 | Shared docs theme/nav includes AuraGlass tokens, AuraOne palette, standard nav, sidebar, Algolia, footer links. | Agent 2 | Complete for local scaffold/build; external Algolia app blocked | Docusaurus config and CSS include AuraGlass token bridge, nav/sidebar/footer, conditional Algolia DocSearch config, and successful static build. | `pnpm --dir opensource/open-studio-platform/docs-template run build`; next action: provision live Algolia credentials/indices. |
| NC-13-03 | 13.3 | Docs hosted at `https://<flagship>.auraone.ai/docs/` via Vercel. | Flagship teams / Agent 4 | External blocker, 2026-05-13 | Hosting not provisioned/verified. | Next action: provision Vercel docs deployments. |
| NC-13-04 | 13.4 | Shared Algolia app with three indices and cross-product search badges. | Agent 2 / external Algolia admin | External blocker, 2026-05-13 | Algolia app/indices not verified. | Next action: create indices and DocSearch config. |
| NC-13-05 | 13.5 | Docs are versioned in lockstep with releases; cross-references version-pinned in MDX. | Agent 2 / flagships | Complete for local release gate; hosted release execution pending | `versions.json`, `versioned_docs/`, and `versioned_sidebars/` are present. `scripts/sync-docs-version.mjs` creates/verifies versioned Docusaurus docs for a release version and rejects unpinned `latest`/`next`/`current` docs references. The release workflow template runs the check against `${GITHUB_REF_NAME#v}` before publishing release artifacts. Hosted release execution remains pending until tagged CI runs. | `node --test opensource/open-studio-platform/tests/docs_version_sync_test.mjs`; next action: run the tagged release workflow and verify the docs gate against the release tag. |
| NC-14-01 | 14.1 | Support SLA response/resolution targets are published. | Agent 1 | Complete | `SECURITY.md` and GitHub templates cover reporting; PRD retains full SLA table. | `rg -n "Response|SEV|business days" opensource/open-studio-platform/SECURITY.md docs/prds/open-studio-platform-prd.md` |
| NC-14-02 | 14.2 | `SECURITY.md` includes what to report, PGP/fingerprint, timelines, safe harbor, credit policy. | Agent 1 / external security owner | Complete for local policy; real PGP publication external | `SECURITY.md` documents what to report, private disclosure routing, PGP publication URL, response targets, safe harbor, outbound destinations, and researcher credit policy. `security/contacts/pgp-placeholder.asc` keeps the missing PGP fingerprint/key explicit until the real key is published. | `node --test opensource/open-studio-platform/tests/security_policy_test.mjs`; next action: publish the real security PGP key/fingerprint at `https://auraone.ai/open/security/pgp.txt`. |
| NC-14-03 | 14.3 | Maintainer list lives at `https://auraone.ai/open/maintainers`. | Developer Relations / Platform owner | Complete for local docs-template page; live publication external | `docs-template/docs/maintainers.md` defines the required maintainer page content for Platform, Security, AuraGlass, Docs, and all three flagship leads, and `sidebars.ts` links it. Live `https://auraone.ai/open/maintainers` publication remains external. | `node --test opensource/open-studio-platform/tests/maintainers_page_test.mjs`; next action: publish the page at the live URL and mirror named GitHub team membership. |
| NC-14-04 | 14.4 | Community channels: GitHub Issues/Discussions, Discord, security email, partners email. | DevRel / Agent 1 | Complete for local channel docs; live provisioning external | `community/channels.md` documents GitHub Issues, GitHub Discussions, Discord channels for all three flagships and platform, `security@auraone.ai`, and `partners@auraone.ai`. `community/github-discussions-seeds.md` includes seed threads for Rubric, Robotics, and Agent. Live Discord/GitHub Discussions provisioning remains external. | `node --test opensource/open-studio-platform/tests/community_channels_test.mjs`; next action: enable Discussions on each repo and create the Discord channels. |
| NC-15-01 | 15.1 | Platform security checks: dependency scan, secret scan, Semgrep, NOTICE/license, cert expiry, updater rotation, crash scrub tests, telemetry CI, keychain matrix, intake schema tests, CSP, ACL audit, mTLS staging, pen-test artifact. | Agent 5 / Agent 3 / Agent 4 | Code-side gates complete; external/account-gated scans and release evidence pending | Security/compliance configs and workflow scaffolds exist. The 2026-05-19 local security refresh confirms `security:all`, product-neutral NOTICE readiness, npm license scan across 1104 packages, Semgrep custom rules, current-tree gitleaks for Platform/Rubric/Robotics/Agent, release-flow contracts, cargo audit, Linux matrix, winget live preflight, and release-blocker preflight now pass locally. `security:notice` verifies the shared NOTICE template, license posture docs, and MIT license files for Platform/Rubric/Robotics/Agent; per-product release-specific NOTICE files remain pending until final signed GA artifact packaging because they must be generated from final SBOM/license reports. The full git-history gitleaks gate still fails with 33 redacted historical findings. Full Snyk/GHAS, Semgrep hosted run, cert expiry, updater key rotation, OS keychain matrix execution, staging mTLS, and third-party pen-test artifact remain external/release evidence. | `docs/evidence/product/open-studio-local-security-release-preflight-2026-05-19.md`; `pnpm --dir opensource/open-studio-platform run security:notice`; next action: remediate or baseline historical gitleaks findings and run hosted security workflows after external services/secrets exist. |
| NC-15-02 | 15.1 | Per-flagship checks: threat model, permission/network/telemetry audits, no-network mode, signing/notarization/update/crash/telemetry/keychain/intake/sidecar/shell exposure checks, release-note sign-off. | Agent 5 / flagship teams | Code-side checklist and local release-flow contracts complete; GA build execution pending | The shared `security/checklists/flagship-handoff-security-review.md` now enumerates the PRD-required per-flagship gates, release-note sign-off evidence, sidecar subprocess limits, and approved Tauri shell exposure. Rubric, Robotics, and Agent each have GA checklist artifacts under `security/checklists/`. The 2026-05-19 release-flow contract run passed 23/23 and verifies no-network telemetry logging, crash default-off scrubbing, network/permission defaults, intake preview/cancel/upload-request separation, and retry classification. Actual GA checklist execution remains pending until flagship release builds exist. | `docs/evidence/product/open-studio-local-security-release-preflight-2026-05-19.md`; next action: run the checklist against each GA build artifact and attach reviewer sign-off. |
| NC-15-03 | 15.2 | Third-party audit scope covers Tauri shell, Rust core crates, signing, update, intake; repeated annually or major release. | Agent 5 / external auditor | External blocker, 2026-05-13 | No audit artifact exists. | Next action: contract audit vendor and attach report. |
| NC-15-04 | 15.3 | Bug bounty starts at Robotics GA on HackerOne or Intigriti with stated tiers. | Agent 5 / external program owner | External blocker, 2026-05-13 | No bounty program exists. | Next action: create draft program and launch at Robotics GA. |
| NC-16-01 | 16.1 | Sequencing locked: Rubric Q1, Robotics Q2, Agent Q3; next starts after prior clears P2. | Agent 1 / all flagships | Complete for PRD alignment | Cross-PRDs refer to shared sequencing. | `rg -n "Q1|Q2|Q3|P2|Platform substrate" docs/prds/*open-prd.md` |
| NC-16-02 | 16.2 | Platform v0.1/v0.2/v0.3 implementation maps. | Agent 1 | Complete | This matrix separates AC-21/22/23 and section coverage. | `rg -n "AC-21|AC-22|AC-23|NC-16-02" opensource/open-studio-platform/IMPLEMENTATION_MATRIX.md` |
| NC-16-03 | 16.3 | Platform lead is shared with Rubric lead through v0.1 and is never also a flagship-feature engineer. | Platform management | Pending staffing decision | No named staffing artifact exists. | Next action: name platform owner in governance docs and CODEOWNERS after org team exists. |
| NC-16-04 | 16.4 | Sync procedure: platform tag, sync PRs, 5-business-day merge, CI gate blocks lagging flagships. | Agent 6 / Agent 4 / Agent 1 | Complete for reusable CI gate contract and local flagship manifests; hosted PR automation pending | Governance/PRD describe tag + sync PR + 5-business-day merge; `.github-templates/workflows/flagship-template-sync.yml` blocks releases with `scripts/check-template-sync.sh`; tests verify fresh sync acceptance, version mismatch failure, and overdue sync failure; Rubric, Robotics, and Agent trees now carry `.open-studio-platform-sync.json` manifests for platform `0.3.0`. | `node --test opensource/open-studio-platform/scripts/test/check-template-sync.test.mjs`; `bash opensource/open-studio-platform/scripts/check-template-sync.sh opensource/rubric-studio-open/.open-studio-platform-sync.json`; `bash opensource/open-studio-platform/scripts/check-template-sync.sh opensource/robotics-studio-open/.open-studio-platform-sync.json`; `bash opensource/open-studio-platform/scripts/check-template-sync.sh opensource/agent-studio-open/.open-studio-platform-sync.json`; next action: wire the reusable workflow into each flagship release workflow after repos are ready. |
| NC-16-05 | 16.5 | RFC process: open RFC under `rfcs/<NNNN>-<title>.md`, problem/proposal/alternatives, Platform owner + one flagship lead sign-off, release + consume. | Agent 1 | Complete | `RFC_PROCESS.md`, `rfcs/0000-template.md`, `GOVERNANCE.md`, `.github-templates/RFC_TEMPLATE.md`. | `test -f opensource/open-studio-platform/RFC_PROCESS.md && test -f opensource/open-studio-platform/rfcs/0000-template.md && test -f opensource/open-studio-platform/.github-templates/RFC_TEMPLATE.md` |
| NC-17-01 | 17.1 | Team allocation: Platform lead 1.0, designer 0.5, flagship integration time 0.25 each. | Management | Pending staffing decision | PRD documents budget; no staffing artifact exists. | Next action: assign named owners. |
| NC-17-02 | 17.2 | Shared infra annual budget lines. | Management / finance | External blocker, 2026-05-13 | PRD documents budget; procurement not verified. | Next action: approve budget and provision services. |
| NC-18-01 | 18.1 | Drift mitigation via weekly platform sync, CODEOWNERS review, quarterly health review. | Agent 1 / Platform owner | Complete for local operating process and public flagship enforcement | `docs/platform-operating-cadence.md` documents the 15-minute weekly platform sync with Rubric/Robotics/Agent leads, required drift agenda/output, CODEOWNERS branch-protection requirements, quarterly health review scope/output, and release-blocking drift rules. `GOVERNANCE.md` links to the cadence doc. Recurring calendar events remain external. 2026-05-19/2026-05-20 cross-repo routing provisioned the security and per-flagship maintainer teams for Robotics, Rubric, and Agent; public Rubric, Robotics, and Agent branch protection now requires two approving reviews plus CODEOWNERS review, and Robotics PR #33/#34/#35 plus Agent PR #8/#9/#10/#11 are merged. | `node --test opensource/open-studio-platform/tests/platform_operating_cadence_test.mjs`; `docs/evidence/product/open-studio-cross-repo-codeowners-routing-2026-05-19.md`; `pnpm --dir opensource/open-studio-platform run verify:dco-enforcement`; next action: complete platform-owner team mapping and root private-repository branch-protection verification with an account/plan that can read the API. |
| NC-18-02 | 18.2 | Flagship experimental escape hatch under `<flagship>/src/experimental/` and RFC contribution path. | Agent 1 / flagships | Complete for governance | `GOVERNANCE.md` documents experiment boundary. | `rg -n "experimental|RFC" opensource/open-studio-platform/GOVERNANCE.md` |
| NC-18-03 | 18.3 | AuraGlass lag mitigated by 0.5 FTE designer and `aura-ide-kit` incubation. | Agent 2 / management | Partial | `packages/aura-ide-kit/` exists; staffing evidence is not present. | Next action: name/staff designer and verify package build. |
| NC-18-04 | 18.4 | Tauri stability mitigated by platform-wide pin, quarterly bump, release monitoring, private fork if needed. | Agent 2 / Platform owner | Complete for local policy and pin enforcement | Template npm and Rust Tauri dependencies are exact patch pins, `verify-platform-contracts.mjs` fails on unpinned Tauri versions, and `docs/platform-template-sync.md` documents quarterly bump, canary sync, release monitoring, revert, and private-fork RFC procedure. | `node opensource/open-studio-platform/scripts/verify-platform-contracts.mjs`; next action: run the quarterly process when the next Tauri bump is proposed. |
| NC-18-05 | 18.5 | Signing compromise mitigated by HSM, two-person procedure, backups, revocation rehearsal, incident playbook. | Agent 4 / Agent 5 | Complete for local runbook contract; HSM/rehearsal external | `security/runbooks/hsm-signing-operations.md` documents HSM operation, two-person approval, storage, backup handling, and audit evidence. `security/runbooks/signing-key-incident-response.md` documents SEV-1 containment, key-specific revocation/rotation, sealed-envelope backup handling, user protection, recovery, annual rehearsal, and closure criteria. Actual HSM provisioning and live revocation rehearsal remain external. | `node --test opensource/open-studio-platform/scripts/test/signing-runbooks.test.mjs`; next action: run a non-production revocation rehearsal after keys and HSM exist. |
| NC-18-06 | 18.6 | Intake schema churn mitigated by versioned schema, Cloud accepts last three majors, 18-month deprecation, CI validators. | Agent 3 / Cloud team | Complete for local schema policy and validators; Cloud last-three acceptance external | `schemas/intake-schema-policy.json` records current accepted major(s), the last-three-major Cloud rule, 18-month breaking-change deprecation window, and additive-minor policy; CI tests verify v1.0/v1.1/v1.2 additive role history and policy constants. Local Cloud queue consumer wiring exists, but last-three-major acceptance still requires staging/live Cloud import API verification. | `node --test opensource/open-studio-platform/tests/intake_schema_policy_test.mjs`; next action: verify the deployed Cloud import endpoint accepts the last three schema majors. |
| NC-18-07 | 18.7 | Cross-platform inconsistency mitigated by macOS/Windows/Linux CI builds and smoke tests. | Agent 4 / Agent 2 | Code-side workflow complete; hosted execution blocked by runner budget/hardware evidence | `.github-templates/workflows/platform-code-verification.yml` defines macOS/Windows/Linux Tauri template verification, native keychain smoke tests, GPU fixture contracts, and local release-flow code gates. The root `.github/workflows/open-studio-platform-hosted-smokes.yml` repeatedly creates all hosted jobs and now supports dynamic `runs-on` JSON via workflow-dispatch inputs and `AURAONE_PLATFORM_*_RUNS_ON` repository variables. Historical root run `26166265859` proves the macOS native keychain smoke once, while sampled root run `26167421304` failed all 10 hosted-smoke jobs before runner steps with the Actions-budget annotation. Robotics public hosted runs are now separately preflighted: CI/accessibility/security runs are readable, and branch protection now requires two reviews, CODEOWNERS, DCO, security/SBOM/license, accessibility/performance, and every declared PRD OS context. PR #33 is merged and fresh `main` run `26158186545` proves the required non-GPU OS matrix, including `CI / macos-x64`. `verify:robotics-hosted-hardware` still reports `readyForRoboticsHostedHardwareClosure: false` because `CI / gpu-video-embedding` is skipped, `ROBOTICS_GPU_CI_ENABLED` is absent, no online self-hosted `linux/x64/gpu` runner is registered, and target hardware/14-day evidence is missing. | `docs/evidence/product/open-studio-hosted-matrix-2026-05-19.md`; `docs/evidence/product/robotics-hosted-hardware-readiness-2026-05-19.md`; `docs/evidence/product/robotics-codeowners-review-routing-2026-05-19.md`; `docs/evidence/product/open-studio-cross-repo-codeowners-routing-2026-05-19.md`; `pnpm --dir opensource/open-studio-platform run verify:root-hosted-actions`; `pnpm --dir opensource/open-studio-platform run verify:robotics-hosted-hardware`; next action: restore root hosted Actions budget or configure OS/GPU self-hosted runner labels, then attach hardware/14-day evidence. |
| NC-18-08 | 18.8 | Telemetry leakage mitigated by opt-in default, schema CI, event log, owner review, grep lint for suspicious names. | Agent 3 / Agent 5 | Complete for local schema/event-log/security checks and public flagship owner enforcement | Telemetry crate tests, platform contract tests, and security forbidden-field scans passed. 2026-05-19/2026-05-20 cross-repo routing provisioned the public flagship security and maintainer teams; Rubric, Robotics, and Agent enforcement is active on `main` with two approving reviews plus CODEOWNERS review. Root private-repository owner enforcement remains externally unverifiable from this account due to the GitHub API/plan limitation. | `pnpm --dir opensource/open-studio-platform test`; `pnpm --dir opensource/open-studio-platform security:all`; `docs/evidence/product/open-studio-cross-repo-codeowners-routing-2026-05-19.md`; `pnpm --dir opensource/open-studio-platform run verify:dco-enforcement`; next action: complete platform-owner team mapping and root branch-protection evidence. |
| NC-18-09 | 18.9 | Update outage mitigated by cached R2 manifests, minimal Worker logic, fallback `updates2.auraone.ai`. | Agent 4 | Complete for live primary/fallback routes | Update Worker serves static R2-backed JSON with minimal rollout/kill-switch logic, both `updates.auraone.ai/*` and `updates2.auraone.ai/*` are live, and platform contract tests verify Tauri updater config includes the secondary endpoint. | `pnpm --dir opensource/open-studio-platform/packages/platform-contracts build && node opensource/open-studio-platform/tests/platform-contracts.test.mjs`; `pnpm --dir opensource/open-studio-platform smoke:live`. |
| NC-18-10 | 18.10 | DCO friction mitigated by no CLA, docs, git alias guidance, and required DCO checks. | Agent 1 / external GitHub admin | Complete for local docs/check and public flagship enforcement | `CONTRIBUTING.md` and `compliance/dco/` document DCO, no CLA, `git commit -s`, and a `git config --global alias.cs "commit -s"` convenience alias. The local DCO checker exists, Rubric, Robotics, and Agent branch protection require `DCO / dco`; Agent PRs #8/#9/#10/#11 and Robotics PRs #33/#34/#35 are merged with passing DCO checks. Root branch-protection evidence remains externally unverifiable from this account. | `node --test opensource/open-studio-platform/tests/dco_guidance_test.mjs`; `docs/evidence/product/open-studio-dco-enforcement-2026-05-20.md`; `pnpm --dir opensource/open-studio-platform run verify:dco-enforcement`; next action: require/verify `DCO / dco` on root once branch protection can be read, and attach root branch-protection evidence. |
| NC-19-01 | 19 | Open questions decisions are recorded and future changes go through RFC. | Agent 1 / platform owner | Complete for process; questions still open by design | RFC process exists; open questions remain in PRD. | `rg -n "Open questions|RFC" docs/prds/open-studio-platform-prd.md opensource/open-studio-platform/RFC_PROCESS.md` |
| NC-20-01 | 20 | Glossary terms are canonical across flagships. | Agent 1 / all flagships | Complete for PRD alignment | Cross-PRDs reference Open Studio Platform and edition terminology. | `rg -n "Rubric Studio Open|Robotics Studio Open|Agent Studio Open|Open Studio Platform|DCO|MIT" docs/prds/*open-prd.md` |
| NC-24-01 | 24 | References include flagship PRDs, AuraGlass, Tauri, Apple, Microsoft signing, Cloudflare, Sentry, Docusaurus, DCO, Contributor Covenant. | Agent 1 | Complete in PRD | References remain in `docs/prds/open-studio-platform-prd.md`. | `sed -n '1660,1685p' docs/prds/open-studio-platform-prd.md` |

## Agent 1 Deliverables

| Deliverable | Status | Evidence | Verification |
|---|---|---|---|
| Governance root files | Complete | `README.md`, `LICENSE`, `CHANGELOG.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `GOVERNANCE.md`. | `find opensource/open-studio-platform -maxdepth 1 -type f | sort` |
| DCO policy | Complete locally; public flagship enforcement active | `CONTRIBUTING.md`; Robotics, Rubric, and Agent branch protection require `DCO / dco`; Agent PRs #8/#9/#10/#11 and Robotics PRs #33/#34/#35 are merged with passing DCO checks. Root branch protection remains externally unverifiable from this account. | `rg -n "DCO|Signed-off-by|git commit -s|no CLA" opensource/open-studio-platform/CONTRIBUTING.md`; `docs/evidence/product/open-studio-dco-enforcement-2026-05-20.md`; `pnpm --dir opensource/open-studio-platform run verify:dco-enforcement` |
| CODEOWNERS | Complete locally; public flagship team routing enforced | `CODEOWNERS`. Robotics, Rubric, and Agent maintainer teams plus `auraoneai/security` now exist and have repo access on their public repositories. Agent, Rubric, and Robotics CODEOWNERS are merged, and branch protection requires two reviews plus CODEOWNERS review on all three public repos. Platform-owner team mapping remains separate. | `rg -n "@auraone/platform-owner|@auraone/security|@auraone/auraglass|@auraone/docs" opensource/open-studio-platform/CODEOWNERS`; `docs/evidence/product/open-studio-cross-repo-codeowners-routing-2026-05-19.md`; `pnpm --dir opensource/open-studio-platform run verify:dco-enforcement` |
| RFC process | Complete | `RFC_PROCESS.md`, `rfcs/0000-template.md`, `.github-templates/RFC_TEMPLATE.md`. | `test -f opensource/open-studio-platform/RFC_PROCESS.md && test -f opensource/open-studio-platform/rfcs/0000-template.md` |
| GitHub templates | Complete for issue/PR/RFC/security routing | `.github-templates/`. | `find opensource/open-studio-platform/.github-templates -type f | sort` |
| Coverage matrix | Complete | `IMPLEMENTATION_MATRIX.md`. | `rg -n "AC-21|AC-22|AC-23|NC-" opensource/open-studio-platform/IMPLEMENTATION_MATRIX.md` |

## External Blockers Requiring Dated Follow-Up

Closed or superseded since the prior audit:

| Item | Closed date | Evidence |
|---|---:|---|
| Linux package artifacts | 2026-05-20 | Agent, Rubric, and Robotics now have signed Linux AppImage/deb/rpm assets for their current release matrix, signed package-repo metadata, clean-keyring verification, and Ubuntu/Fedora clean-install evidence; `pnpm --dir opensource/open-studio-platform verify:linux-artifacts` reports `readyForLinuxArtifactClosure: true`. |
| Cloudflare telemetry DNS/local resolver blocker | 2026-05-20 | `updates.auraone.ai`, `updates2.auraone.ai`, `intake.auraone.ai`, `install.auraone.ai`, `schemas.auraone.ai`, and `o.auraone.ai` respond from Cloudflare, and `pnpm --dir opensource/open-studio-platform smoke:live` passes after deployment. |
| Sentry org/projects and DSNs | 2026-05-20 | `docs/evidence/product/open-studio-sentry-2026-05-20/` records the `auraone-open` Sentry projects, DSNs, scrub configuration, synthetic uploads, dashboards, and Rubric uptime monitor evidence. |
| Public blocker issue routing | 2026-05-20 | `pnpm --dir opensource/open-studio-platform run verify:public-blockers` reports `readyForPublicBlockerRoutingClosure: true`; Rubric issues #42-#47, Agent issues #2-#7, and Robotics issues #10-#32 all have the expected public issue numbers, open/closed states, and labels. |

| Blocker | Date recorded | Exact next action |
|---|---:|---|
| Windows EV, managed signing, YubiHSM, and Microsoft package identity evidence | 2026-05-19 | Microsoft package identities are now reserved and verified for all three flagships. Procure/provision Windows signing through EV cert/thumbprint, PFX, or managed Azure Artifact Signing custody, move Apple/GPG/Tauri signing custody into CI/HSM, then run hosted signing canaries and `verify:windows-package-identity`. 2026-05-20 capture templates for all required Windows identity/signing/install evidence keys are generated by `pnpm --dir opensource/open-studio-platform run windows-evidence:templates` under a verifier-ignored `templates/` subtree. |
| winget publication | 2026-05-19 | Produce EV-signed MSI artifacts, replace placeholder SHA/ProductCode values, capture clean Windows install QA, and submit winget manifests. Homebrew casks are published. 2026-05-20 winget EV/submission capture templates are generated by `pnpm --dir opensource/open-studio-platform run windows-evidence:templates`; they are not counted as evidence until replaced by real release output. |
| Robotics hosted hardware matrix | 2026-05-20 | PR #33 is merged and public `main` CI run `26158186545` proves the required non-GPU OS matrix including `CI / macos-x64`. Register an online self-hosted `linux/x64/gpu` runner, enable `ROBOTICS_GPU_CI_ENABLED=true`, attach M2 Pro/Linux x86/Windows 11 baselines plus GPU fixture and 14-day green history evidence under `AURAONE_ROBOTICS_HARDWARE_EVIDENCE_DIR` or `docs/evidence/product/robotics-hosted-hardware/`, and rerun `verify:robotics-hosted-hardware`. Branch protection now requires the full declared PRD OS context set. |
| Launch operations execution | 2026-05-19 | Local launch packets/assets are prepared and verified by `pnpm --dir opensource/open-studio-platform run verify:launch-ops`; 2026-05-20 capture templates for all 22 external launch actions are generated by `pnpm --dir opensource/open-studio-platform run launch-ops:templates` under a verifier-ignored `templates/` subtree; schedule/post/send/upload/community/on-call/briefing evidence remains external under `AURAONE_OPEN_STUDIO_LAUNCH_EVIDENCE_DIR` or the committed evidence directory. |
| DCO enforcement and CODEOWNERS team provisioning | 2026-05-20 | Public flagship CODEOWNERS teams are created and attached for Robotics, Rubric, and Agent. Rubric, Robotics, and Agent CODEOWNERS are merged and protected; branch protection requires `DCO / dco`, two approving reviews, and CODEOWNERS review. `verify:dco-enforcement` records the gate and confirms Agent PRs #8/#9/#10/#11 plus Robotics PR #33/#34/#35 are merged with passing `DCO / dco`. Next action: map platform-owner teams/root branch protection with an account or plan that can verify the private root API. |
| PGP key for security disclosures | 2026-05-13 | Generate key, publish armored public key/fingerprint in `SECURITY.md`, and store private key. |
| Third-party security audit and bug bounty | 2026-05-13 | Contract audit vendor before Rubric GA and draft HackerOne/Intigriti program for Robotics GA. |

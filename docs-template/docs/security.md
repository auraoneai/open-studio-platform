# Security

AuraOne Open Studio flagships inherit the shared Tauri CSP, `auraone://` URL scheme, updater contract, keychain API, crash opt-in, and intake packet review flow from the Open Studio Platform.

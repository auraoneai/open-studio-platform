# Privacy And Telemetry

Telemetry is opt-in only. The app records a local event log showing every event that would be sent or has been sent in the current session.

The platform never sends project content, API keys, file paths, hostnames, prompts, rubrics, samples, traces, display names, or emails in telemetry events.
